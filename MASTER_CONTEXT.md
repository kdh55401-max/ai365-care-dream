# MASTER_CONTEXT — AI365 CARE DREAM (이 워크트리, 음성 UX 분석 추가분)

기준일: 2026-09-10 · 워크트리: `elastic-curran-a97171` · 브랜치: `claude/caregiver-screen-redesign-f5fc74`
· HEAD `0c8410a` (당시 `origin/master`와 동일 — diff 없음, 아래 §1 참고)

**이 문서는 저장소의 단일 정본이 아니다.** 더 상세한 정본 MASTER_CONTEXT.md/AGENTS.md는
`ai365-care-dream-handoff-4a85b3` 워크트리(Claude/Codex/Astra 협업 인계 체계)에 있으며, 이번
세션은 그 워크트리의 우발적 동시수정을 피하기 위해 그쪽은 **읽기 전용**으로만 참고하고, 이번
분석에서 확정된 음성 UX 설계 원칙만 여기에 압축해 남긴다. 두 문서를 병합할지는 사용자가 결정한다.

## 1. 브랜치·HEAD·배포 버전 구분 (2026-09-10 확인)

- **이 워크트리/`origin master`**: `0c8410a` — 홈 화면 재구성, 무관 발화 처리, 관리자 "미분류"
  집계, AI 폴백 추적까지 반영됨. 공개 배포(Vercel)가 이 커밋을 실제로 서비스 중인지는 이
  환경에서 확인 불가(Vercel 로그인 없음, 실제 URL 미보유).
- **`ai365-care-dream-handoff-4a85b3` 워크트리** (읽기 전용 확인, 2커밋 앞섬 + 다수 미커밋):
  `0c8410a` 위에 `2425a4c`(CD-01/CD-02 수정: 관찰영역 세분화, 수급자별 완료 판정)와
  `bc7e36d`(데모 이탈 시 demo=1 유실 수정, not_observed/uncertain 문구 분리)가 로컬 커밋으로만
  존재 — **master에 병합되지 않았고 배포되지도 않았다.** 즉, 이 저장소의 "최신 통합 코드"는
  두 버전으로 갈라져 있다: master(배포 후보)와 handoff(더 진전됐지만 미병합).
- 아래 표는 **master(`0c8410a`) 기준**으로 판정하되, handoff 워크트리에 이미 검증된 수정이
  있으면 그 사실을 각주로 명시한다(같은 결함을 다시 고치지 않기 위함).

## 2. 오늘 확정한 음성 UX 설계 원칙 (Master 18개 항목 요약, 코드 변경 없음 — 분석만)

- "60초"는 제한이 아니라 목표라는 문구로만 표현한다(이미 반영 — `CareApp.tsx`).
- 추가질문은 0회도 정상이다. 필요할 때만 보통 1~3개, 이미 답한 항목은 재질문하지 않는다
  (기존 원칙 유지, 새로 만들지 않음).
- "평소와 같아요"는 그 자체로 유효한 보고다. 언급 안 된 항목을 정상으로 채우지 않는다. 상태는
  평소와 같음/변화/미관찰/불확실 4종을 유지하고 재작성하지 않는다.
- 음성은 중심 입력수단이되 글 입력·선택 버튼은 항상 대체 수단으로 유지한다.
- **전송 동의는 최종 확인 화면에서 사람이 직접 누르는 명시적 조작으로만 받는다.** 침묵,
  일반 대화의 "네", AI 자신의 음성 출력을 동의로 처리하지 않는다 — 지금은 음성 동의
  기능 자체가 없어 이 안전조건이 자동으로 지켜지고 있으나, 앞으로 음성 동의를 추가할
  때도 이 조건을 반드시 유지한다.
- 듣는 중 / 처리 중 / 질문 중 / 확인 대기 / 전송 중 / 전송 완료는 서로 다른 화면·문구로
  구분하고, 실패를 전송 완료로 표시하지 않는다(기존 원칙 유지).
- 캐릭터(친근함·신뢰감·전문성)는 이번에 이미지·애니메이션을 새로 만들지 않는다 — 방향만
  검토 대상으로 남긴다.
- 응급 안내(119/센터), 대상자 맥락, 배정, 중복 방지, 데모/실증 데이터 분리, 지표 분리는
  모두 기존 그대로 보존한다.
- **핵심 흐름이 5단계로 확장됐다**: 기존 "요양보호사 말하기 → AI 확인 → 관리자 검토"에
  "관리자 응답·조치가 현장에 돌아온다"가 새로 추가됨 — 이 5번째 단계는 현재 코드에
  존재하지 않는다(§3의 결과물 표, C 항목 참고). 이 문서에서 처음 명문화한다.

## 3. 확인 방법

grep으로 `speechSynthesis`/TTS/캐릭터 관련 코드 부재를 직접 확인했고, 관리자 검토 상태
(`review_status`/`review_note`/`reviewed_at`)가 `CareApp.tsx` 어디에서도 읽히지 않음을
확인했다(관리자 → 현장 역방향 연결 없음). 상세 근거와 판정표는 이번 세션 보고(대화 기록) 및
[HANDOFF_TO_CODEX.md](HANDOFF_TO_CODEX.md)를 참고한다.
