## 2026-09-11 — 스크린샷 173·174: 초안 격리·재시도·모바일 UX

기준: origin/master 9fe2b0b. 173의 /care/scenario 문구는 runAiTurn의 원문 PATCH → ai-turn POST → 정리본 PATCH 중 어느 실패에서도 발생하던 공통 안내다. Elements 탭의 오류 개수만으로 실패 요청/원인을 특정할 수 없다. 연결 브라우저에 C07 세션이 없고 당시 Network/서버 로그도 확보되지 않아 그 사건의 HTTP 상태·Gemini 장애 여부는 미확정이다. 공개 도메인 /care 및 /care/scenario는 200/동일 번들, 미인증 session 200(authenticated:false), reports 401을 확인했다. 이는 정상 인증 경계이며 과거 C07 오류의 증거가 아니다.

확인·수정:
- CareApp: 운영/연습 공용 초안 키를 분리하고 서버 report_source와 라우트를 대조해 복원한다. scenario_id를 복원하며 구형 연습 초안도 로컬 별도 키로 보존한다. 실제 서버 기록을 삭제·전환하지 않는다.
- 요청 ref 잠금과 재시도 disabled로 빠른 중복 실행 방지. 원문 저장/AI 응답/정리본 저장/최종 제출 실패 단계를 구분하고 HTTP 상태만 진단한다. 원문·인증정보를 로그에 남기지 않는다. 텍스트 시작 재시도가 마이크를 켜던 옵션 누락도 수정.
- reports.ts + reportRetry.ts: 이미 제출된 동일 최종본 재시도는 성공 결과만 반환한다. 내용이 다르면 409로 보호한다. draft 조건부 갱신으로 제출된 기록 덮어쓰기를 막고 기존 연습 초안을 재사용한다. 데모도 같은 계약을 적용한다.
- 연습 전 화면에 ‘시뮬레이션 · 연습용 상황’ 표시와 실제 화면 복귀 링크. 예시를 실제 관찰로 표현하지 않는다.
- 모바일 아바타/여백만 조정, 글 입력 링크 강조, ‘안내 음성 켜짐’으로 녹음과 구분. 텍스트 입력 포커스 시 안내 낭독 취소. 기존 실제 영상/무음/정지 대체/움직임 줄이기 유지(립싱크 아님).

분리 원칙: demo=1 쿼리만 외부 API 없는 demoCareRepo/localStorage를 선택한다. /care/scenario 단독은 운영 인증·API·DB를 사용하되 report_source=scenario로 저장한다. 관리자 기본 목록/computeStats/참여·누적 지표는 scenario를 제외하고 computeScenarioStats로 별도 집계한다. 질문 횟수·지표 정의·관리자 기능·배정 변경 없음.

검증: build 성공, vitest 119/119(실제 서버 핸들러+모의 DB의 동일 제출 재시도/상이한 내용 보호 2건 추가), lint 오류 0/기존 경고 4. Chrome 모바일 360/390의 관련 24개 시나리오 최종 통과: 최초 20/24 후 테스트 지연 대기 조건 수정 및 느린 환경 시간초과 2건을 재검증하여 6/6 통과. 구형 키 보존 수정 후 격리/작은 화면 4/4 추가 통과. 일반 보고→질문→원문/수정본 확인→제출→데모 관리자 C01/A01 연결, 연습 별도 집계, AI 502 모의 실패/입력 유지/빠른 재시도 1회, 저장 성공 응답 유실 재시도 1건만 저장, 마이크 거부 후 글 입력, TTS 중첩 방지 확인. 기본 Playwright Chromium 바이너리가 없어 설치된 Chrome을 임시 로컬 설정으로 사용했다.

미확인/설정: 실제 C07 실패 요청·Vercel 과거 런타임 로그, 실 API 생성/실 DB 동시성·관리자 운영 수신·실기기 음성은 미검증. 운영에 가짜 보고는 작성하지 않았다. 센터 번호는 VITE_CENTER_PHONE_NUMBER 빌드 환경변수이며 확인된 번호가 없어 미등록 유지. 다음 오류 발생 시 실패 단계/HTTP 상태와 해당 요청 응답·서버 로그를 대조해야 원인을 확정할 수 있다.

## 2026-09-11 — 생성 인사·대기 영상 운영 반영

사용자가 정지 이미지 대신 생성 영상 적용 후 즉시 푸시·배포하도록 명시적으로 요청했다. 원격 master 7621ca9를 기준으로 최신 대화/음성/저장 코드를 유지하고 아바타 미디어만 교체했다.

- public/avatar-welcome.mp4, avatar-idle.mp4, avatar-poster.webp: 실제 Higgsfield 생성 후 보정한 5초·512p·무음 자산. 인사 1회 후 대기 반복. 원본 명찰 합성 복구 및 대기 손 자세/루프 경계 보정.
- AvatarVideo.tsx: welcome→idle, muted/playsInline, autoplay/error/초기 재생 시간초과 poster, reduced-motion 우선 정지. AiAvatar의 기존 위치/크기 전환과 실제 앱 상태 표시 유지.
- 기존 CareApp/speechOutput/API/DB/admin/shared 변경 없음. 실제 립싱크 없음.
- npm run build 성공, npm test 117/117, lint 오류 없음(경고 4개). 사용자 요청에 따라 상세 오류/실기기 음성/운영 DB 검토는 배포 이후로 남긴다.

# HANDOFF_TO_CODEX.md — 이 워크트리 (elastic-curran-a97171)

## 2026-09-11 — 사용자 제공 아바타와 이어지는 대화 화면

이번 사용자의 지시에 따라 과거의 추상 원 대신 첨부한 인물 아바타 정지 이미지를 원본 그대로 적용했다. 큰 홈 아바타가 같은 DOM을 유지하면서 620ms 동안 상단 80px 아바타로 축소·이동한다. 현재 배정 대상, 자유 입력, 실제 대화 이력, 현재 AI 질문, 원문과 수정 가능한 보고문, 명시적 센터 보고를 하나의 흐름으로 연결했다.

TTS 실제 이벤트와 마이크 실제 시작 이벤트를 구분한다. AI 요청 실패·재시도 및 중단 후 새로고침 복구, 무변화 문답 로컬 초안 복원을 보완했다. 새 이미지/영상 생성과 립싱크는 없다. 관리자/서버 인증/DB/지표 계산은 변경하지 않았다.

기준은 master 703d502이며 기존 미커밋 변경은 없었다. 구현·모바일 캡처·전환 영상·검증 결과는 [CODEX_CAREGIVER_REVIEW.md](CODEX_CAREGIVER_REVIEW.md)에 모았다. 아래의 '미병합/미배포' 및 '얼굴 없는 아바타' 내용은 과거 기록이다. 실제 API/DB 인증 통합과 실기기 음성·키보드는 검증 완료로 간주하지 않는다.


가장 최근 항목이 위에 온다. 이 워크트리는 `ai365-care-dream-handoff-4a85b3`(정본 인계
체계)와 별도다 — 두 문서를 병합할지는 사용자가 결정한다.

---

## 2026-09-10 (3차) — 요양보호사 화면을 "돌봄 AI와 대화하는 경험"으로 개선: Codex 독립 검증 요청

### 목적과 기대 동작
Master 지시("Master의 방향은 확정됐다... 분석 보고만 반복하지 않는다... 아바타·대화
화면 개선을 또 후순위 제안으로만 남기지 말고, 이번 구현 결과로 보여줘")에 따라 아래
1차 인계(2026-09-10, 음성 UX 18개 항목 분석)의 "제안하지 않은 것" 중 아바타/TTS를
이번에 실제로 구현했다. 코드를 직접 검증하고, 범위 내(이번 커밋이 건드린 파일)에서
발견한 오류는 직접 고쳐도 좋다 — 새 기능 확장이나 Gemini 질문 프로토콜/모델 공급자
변경은 범위 밖이다(별도로 표시해 달라).

### 기준 브랜치·커밋
- 브랜치: `claude/caregiver-screen-redesign-f5fc74`, 이 문서 작성 시점 HEAD `d505313`.
- 이 커밋은 `origin`에 아직 push하지 않았다 — 이 인계의 Codex 검증을 거친 뒤에만
  `master`로 병합·배포를 진행한다(사용자 지시, 기존 절차와 동일).
- 부모 커밋 `2985011`(origin/master와 동일, `/support` 별칭 반영 이후) 위에 이번 커밋
  하나만 쌓았다 — 이번 커밋 전체가 신규 검토 대상이다.

### 수정 파일과 핵심 변경 내용 (`d505313`)
- `src/pilot/care/AiAvatar.tsx`(신규): 상태 기반 추상 아바타. 얼굴·눈·입 없음(의도적).
  **Codex 확인 요청**: "유아용/게임/동물 캐릭터로 보이지 않을 것", "의료 자격을 가진
  사람으로 오인되지 않을 것"이라는 두 제약을 실제로 만족하는지 디자인 관점에서 재검토.
- `src/pilot/care/ConversationLog.tsx`(신규): `CareApp.tsx`의 `buildQuestionTurns()`/
  `buildNoChangeTurns()`가 넘기는 턴만 렌더링. **Codex 확인 요청**: 이 두 빌더 함수가
  실제로 `rawInput`/`followupHistory`/`noChangeQaHistoryRef`에서만 턴을 만드는지,
  하드코딩되거나 예시 텍스트가 섞여 들어가는 경로가 없는지 재확인.
- `src/pilot/care/speechOutput.ts`(신규): Web Speech Synthesis 래퍼. 실패 시 항상
  `onError`만 호출하고 예외를 던지지 않는다. **Codex 확인 요청**: `CareApp.tsx`의
  자동 낭독 `useEffect`(screen/currentQuestion/noChangeStep 의존)가 화면 전환마다
  중복 호출되지 않는지(`lastSpokenTextRef` 가드가 모든 재진입 경로를 커버하는지),
  특히 같은 질문으로 되돌아오는 경로(에러 후 재시도 등)에서 의도치 않게 조용해지거나
  반대로 반복 낭독되지 않는지.
- `src/pilot/care/CareApp.tsx`: 홈/대화/reportReview/submitted 화면 재구성(아바타 삽입,
  단일 시작 버튼, 문구 교체, 텍스트입력/기록목록 보조 위치화) + 버그 수정 3건(기록·
  질문 화면 마이크 `reconnecting`→`resume()`, reportReview "확인된 영역"
  `splitDomainsByStatus` 분리). **Codex 확인 요청**: `getAvatarState()`의 상태 우선순위
  (error > listening > loading > screen별 분기)가 실제로 있을 수 있는 모든 조합에서
  합리적인 상태를 고르는지 — 예를 들어 `voice.state==='listening'`이면서 동시에
  `screen==='reportReview'`인 조합이 실제로 발생 가능한지, 발생한다면 어떤 상태가
  더 정확한지.

### 직접 수행한 테스트와 결과
- `npx tsc -b` 0 오류, `npx vitest run` 113/113(신규 테스트 없음 — 이번 변경은 UI
  프레젠테이션 계층이라 기존 단위테스트 범위 밖), `npx oxlint` 기존 경고만(신규
  AiAvatar.tsx fast-refresh 경고 1건, 컴포넌트+상수를 한 파일에서 export해서 발생하는
  하네스 성격의 경고 — 무해), `npx vite build` 성공(64 모듈, 기존 크기와 동일).
- `npx playwright test`(mobile-390/360, 48개) 48/48 통과(전체 스위트, 이번 변경으로
  깨진 시나리오 없음).
- 라이브 브라우저(데모 `c1`/`6003`, 390×844) 직접 재현: 로그인 → 홈(문구 "오늘 A01
  어르신은 어떠셨어요?" 확인, 오디오 토글 44×44px 확인) → "이야기 시작"(이 환경은
  마이크 권한이 차단돼 있어 아바타가 실제로 `error` 상태로 전환되고 "문제가
  생겼어요" + 텍스트 입력 폴백이 뜨는 것까지 확인 — 이는 앱의 실제 오류 처리 경로가
  작동함을 보여준다) → 텍스트로 "오늘 점심을 평소보다 적게 드셨고, 기분은 편안해
  보이셨어요." 입력 후 제출 → 후속 질문 화면에서 전사 로그에 방금 입력한 문장과
  AI 질문("그때 현장에서 어떤 조치를 하셨나요?")만 실제로 표시됨을 확인(가상 대화
  없음) → 선택형 답변 진행 → reportReview에서 `window.speechSynthesis.speak`를
  JS 스파이로 감싸 "요약 듣기" 클릭 시 실제 요약 텍스트("관찰한 돌봄 상황: 오늘
  점심을... 현재 상태: 그대로예요. 센터 확인사항: ...")로 호출되고 `start` 이벤트가
  실제로 발생함을 확인(이 환경에 설치된 한국어 음성 1개 확인) → "이대로 센터에
  보내기" → 완료 화면에서 "이 브라우저에 데모 기록을 저장했어요 · 실제 센터로
  전송하지 않았어요" 고지 확인 → 홈으로 돌아가 `dailySubmitted` 상태("오늘
  돌봄기록을 남겼어요" + "추가 상태변화 기록하기" 버튼 노출, Request A에서 만든
  추가보고 게이팅이 이번 변경 후에도 유지됨을 확인) 전체 경로 재현 완료.
- **실행하지 않음**: 실제 Gemini/Supabase 경로(자격증명 없음), 실기기 음성 인식
  정확도·TTS 실제 음질(이 환경은 헤드리스라 합성음 출력 자체를 청취로 확인 불가,
  API 호출·이벤트 발생만 확인), Vercel 실제 배포 확인.

### 확인할 화면·재현 순서
1. `/care?demo=1` 데모 초기화 → `c1`/`6003` 로그인 → 홈 화면에서 아바타+"이야기 시작"이
   하나의 버튼으로 묶여 있는지, 문구가 실제 배정 대상자 코드를 쓰는지 확인.
2. "이야기 시작" → 마이크 권한을 허용한 환경이라면 실제 음성 인식이 되는지, 차단된
   환경이라면 텍스트 폴백이 자연스럽게 뜨는지 확인.
3. 아무 문장이나 입력해 제출 → 후속 질문 화면에서 상단 아바타가 대화 상태를 반영하고,
   전사 로그에 방금 입력한 내용과 AI 질문만 보이는지(예시 문구 섞임 없음) 확인.
4. 질문 2~3개에 답한 뒤 reportReview 화면에서 "요약 듣기"를 눌러 실제 음성이 나오는지
   (또는 최소한 오류 없이 실행되는지) 확인, "이대로 센터에 보내기"로 제출.
5. 완료 화면의 데모 미전송 고지, 홈 화면의 제출 완료 상태를 확인.
6. `/admin?demo=1`(`demo1234`)에서 방금 제출한 보고가 정상적으로 보이는지(이번 변경이
   관리자 연결을 깨지 않았는지) 확인.

### 남아 있는 불확실성과 Codex에게 요청하는 점검
- 위 "Codex 확인 요청" 4곳(아바타 디자인 제약, 대화 로그 데이터 소스, TTS 중복/누락
  방지, 아바타 상태 우선순위)을 우선 봐 달라.
- TTS는 이 환경에서 API 호출과 `start` 이벤트 발생까지만 확인했다 — 실제 음질/발음/
  속도는 실기기에서 별도 검증이 필요하다(범위 밖으로 명시).
- 통과 판정을 주면 이 브랜치를 `origin/master`로 병합·푸시하고 실제 반영 상태를
  확인해 보고하겠다. 반영/보류를 지적별로 표시해 돌려주면 그대로 처리한다.

---

## 2026-09-10 (2차) — CD-01/CD-02 통합 + 관리자 응답 연결: Codex 독립 검증 요청

### 목적과 기대 동작
Master가 바로 아래(1차) 분석에서 제안한 항목 중 2건을 승인해 구현했다. 코드를 직접
검증하고, 범위 내(review_status/review_note 공개 로직, CD-01/CD-02 관련 코드)에서
발견한 오류는 직접 고쳐도 좋다 — 사업 방향을 바꾸는 제안이나 이번 제외 범위(아래 참고)
확장은 별도로 표시해 달라.

### 기준 브랜치·커밋
- 브랜치: `claude/caregiver-screen-redesign-f5fc74`, 이 문서 작성 시점 HEAD `1d9373f`.
- 이 커밋은 `origin/master`(당시 `0c8410a`)에 아직 push/병합하지 않았다 — 이 인계의
  Codex 검증을 거친 뒤에만 병합·배포를 진행한다(사용자 지시).
- 관련 커밋 4개(오래된 순): `f5de153`(1차 분석 문서만, 코드 없음) → `13e7801`(CD-01/
  CD-02를 `ai365-care-dream-handoff-4a85b3`의 `2425a4c`/`bc7e36d`에서 그대로 병합 —
  이 두 커밋 자체는 그 워크트리에서 이미 검증받았으므로 재검증 우선순위 낮음) →
  `1d9373f`(관리자 응답 연결 신규 구현 — 재검증 우선순위 높음).

### 수정 파일과 핵심 변경 내용 (`1d9373f`만, 신규 구현)
- `shared/careTypes.ts`: `review_note_visible_to_caregiver: boolean` 필드 추가(기본
  false), `normalizeReportRecord`에서 옛 기록은 항상 false로 정규화.
- `db/schema.sql` + `db/migrations/2026-09-10-review-note-visibility.sql`: 위 컬럼을
  `not null default false`로 추가하는 멱등 마이그레이션(관리자가 Supabase에 직접 적용
  필요, 아직 미적용).
- `api/admin/reports.ts`(`handleReviewStage`): 이 컬럼을 명시적 select/기존 update에
  넣지 않고 **별도 best-effort update로 분리**했다 — 마이그레이션 미적용 상태에서도
  검토(승인/반려) 저장 자체가 깨지지 않게 하려는 의도(`ai_fallback_used`와 동일 패턴).
  **Codex 확인 요청**: 이 분리가 실제로 안전한지, 특히 별도 update가 실패했을 때
  `finalReport`가 여전히 정확한(방금 저장된 review_status 등을 포함한) 값인지 확인해
  달라.
- `src/pilot/admin/AdminApp.tsx`: 반려 사유 textarea 옆에 "요양보호사에게 보이기"
  체크박스(기본 unchecked) 추가, 과거 검토 표시부에 공개/비공개 배지 추가.
- `src/pilot/care/CareApp.tsx`: `history`(목록) 화면에 상태 한 줄, `historyDetail`
  화면에 "관리자 확인" 카드 추가 — pending/승인·응답없음/반려·응답있음 3가지 렌더링
  분기. **Codex 확인 요청**: 문구가 "조치 완료"/"돌봄 반영 완료"처럼 과대 해석되지
  않는지, `reviewed_at`을 열람/현장반영 시각으로 오인할 수 있는 표현이 없는지 재확인.
- `src/pilot/demo/demoCareRepo.ts`(`getReport`): 참여자 소유권 검사가 없던 것을
  발견해 추가(다른 참여자 보고 id로 조회 시 404). **Codex 확인 요청**: 같은 파일의
  `patchReport`에는 이 검사가 없다(오늘 범위 밖으로 남겨둠) — draft 상태에서만
  작동하고 현재 UI에서 다른 참여자의 draft id를 얻을 방법이 없어 보이지만, 독립적으로
  재확인해 달라.
- `api/care/reports.ts`: `CARE_LIST_COLUMNS`에 `review_status` 추가(목록 배지용,
  기존 컬럼이라 마이그레이션 이슈 없음).
- `src/pilot/shared/adminRepo.ts`, `src/pilot/demo/demoAdminRepo.ts`: 위 필드를
  `ReviewReportInput`/`reviewReport`에 배선.

### 직접 수행한 테스트와 결과
- `npx tsc -b` 0 오류, `npx vitest run` 113/113(신규 4건: 공개 여부 게이팅 2건,
  소유권 격리 1건, 기존 충돌처리 테스트 확장 1건 — `src/pilot/demo/demoReview.test.ts`),
  `npx oxlint` 기존 경고 2건만, `npx vite build` 성공.
- `npx playwright test`(mobile-390/360, 42개): 41/42 통과. 실패 1건
  (`no-change-and-scenario.spec.ts`의 "정상 상태 구체화", mobile-390)은 단독 재실행 시
  23.3초에 통과 — 부하로 인한 타임아웃으로 판단(관련 코드를 이번에 건드리지 않음).
- 데모 브라우저(C01/A02, C01/A01)로 직접 클릭 재현: CD-01 재현 문구 → 수분이 별도
  문장으로 분리되고 조치/센터확인이 "미응답"으로 남음 확인. A02 제출 후 A01 시작 버튼
  유지 확인(CD-02). 반려+공개체크 → 요양보호사 화면에 사유+응답 시각 노출 확인.
  승인+체크 안 함 → "승인됨"만 보이고 내용 없음 확인.
- **실행하지 않음**: 실제 Gemini/Supabase 경로(자격증명 없음), 실기기 음성/키보드,
  Vercel 실제 배포 확인, 다른 참여자 격리를 실제 두 브라우저 프로필로 재현(대신
  vitest로 저장소 레벨에서 확인).

### 확인할 화면·재현 순서·시험 데이터 조건
1. `/care?demo=1` 데모 초기화 → `c1`/`6003`로 로그인(C01, A01·A02 배정) → "글로
   입력하기" → "식사, 이동, 배설 모두 평소와 같아요. 수분 섭취는 관찰하지 못했어요."
   입력 → 제출까지 진행 → 최종 확인 화면에서 수분이 분리돼 있는지 확인.
2. 같은 브라우저에서 대상자를 A01로 바꿔 시작 버튼이 여전히 있는지 확인(CD-02).
3. `/admin?demo=1`(`demo1234`)로 로그인 → 방금 A02 보고를 열어 반려 사유를 적고
   "요양보호사에게 보이기"를 체크한 뒤 반려 확정.
4. `/care?demo=1`로 돌아가 "내가 남긴 돌봄기록" → 해당 보고를 열어 반려 사유와
   응답 시각이 보이는지 확인.
5. 다른 보고를 하나 더 만들어 이번엔 체크박스를 누르지 않고 승인 → 요양보호사 화면에
   "관리자 확인: 승인됨"만 보이고 본문은 없는지 확인.

### 남아 있는 불확실성과 Codex에게 요청하는 점검
- 위 "Codex 확인 요청" 3곳(best-effort update 안전성, 문구 재확인, patchReport 소유권
  검사 범위)을 우선 봐 달라.
- `ai365-care-dream-handoff-4a85b3`의 미커밋 변경(D6 라벨, `careReportAi.ts` 프롬프트
  실험)은 이번 병합에 포함하지 않았다 — 그 세션이 별도로 커밋한 뒤 다시 대조가
  필요하다.
- 통과 판정을 주면 이 브랜치를 `origin/master`로 병합·푸시하고 실제 반영 상태를
  확인해 보고하겠다. 반영/보류를 지적별로 표시해 돌려주면 그대로 처리한다.

---

## 2026-09-10 — 음성 UX 18개 항목 분석 후 권장 최소 구현 3건 (코드 변경 없음, 제안만)

### 목적과 기대 동작
이번 세션은 코드를 바꾸지 않았다 — Master가 전달한 음성 중심 돌봄 UX 18개 항목을 현재
구현과 대조한 결과, 아래 3건을 "효과가 큰 최소 구현"으로 제안한다. 각 항목은 이미 존재하는
데이터/코드를 최대한 재사용하며, 새로운 대규모 기능(TTS, 캐릭터 애니메이션, 음성 동의)은
포함하지 않는다 — 그런 기능은 실제 음성 출력 연결 없이는 "완성된 것처럼" 제안하지 않는다.

### 기준 브랜치·커밋
- 이 워크트리: `claude/caregiver-screen-redesign-f5fc74`, HEAD `0c8410a` (`origin/master`와 동일).
- 참고(읽기 전용, 병합 안 함): `ai365-care-dream-handoff-4a85b3`의 `bc7e36d`(부모 `2425a4c`) —
  아래 제안 1이 바로 이 커밋을 가리킨다.

---

### 제안 1 (최우선, 위험 최소) — 이미 검증된 CD-01/CD-02 수정 병합

**무엇**: `ai365-care-dream-handoff-4a85b3`의 `2425a4c`+`bc7e36d` 두 커밋을 `master`에
병합한다. 새 코드를 짜는 게 아니라 이미 짜여 있고 검증된 코드를 가져오는 것이다.

- CD-01: `shared/careTypes.ts`/`shared/noChangeEngine.ts` — `meal_hydration`/`mobility_fall`
  통합 도메인을 `meal`/`hydration`/`mobility`/`fall` 4개로 분리. "특이사항 없음" 흐름에서
  조치·센터확인을 묻지 않았으면 "조치 없음"/"확인할 사항 없음"이 아니라 "미응답"/"아직
  검토되지 않음"으로 적음. `not_observed`(관찰 못함)와 `uncertain`(확실하지 않음)의 문장을
  분리.
- CD-02: `src/pilot/care/CareApp.tsx`(`computeDailySubmitted`), `api/care/reports.ts`,
  `src/pilot/demo/demoCareRepo.ts` — 일일보고 "오늘 이미 제출" 판정을 참여자 단위가 아니라
  참여자+수급자 단위로 바꿈.

**왜 지금**: Master의 설계 원칙 #3("평소와 같음·변화·미관찰·불확실 구분, 미응답은 별도
보존")과 정확히 일치하는 수정이 이미 구현·검증까지 끝난 채 배포되지 않고 있다. 다시
구현하면 handoff 워크트리 세션의 작업과 충돌하거나 중복된다.

**확인 기준(Codex/재현자가 확인할 것)**:
1. 병합 전 재현: master(`0c8410a`)에서 데모 로그인 → "이야기 시작" → "식사, 이동, 배설
   모두 평소와 같아요. 수분 섭취는 관찰하지 못했어요." 입력 → 최종 확인 화면에서 `change`
   필드가 "식사·이동·배설·수분 상태는 평소와 유사"처럼 수분까지 뭉뚱그려지는지 확인.
2. 병합 후: 같은 입력으로 수분이 별도 문장("수분 상태는 이번 방문에서 확인하지 못함")으로
   분리되는지 확인. `action`/`escalation` 필드가 "미응답"/"아직 검토되지 않음"으로 나오는지
   (조치를 묻지 않은 케이스).
3. CD-02: 여러 수급자를 배정받은 참여자(예: 데모 C01→A01,A02)로 A02에 일일보고를 제출한
   뒤, 홈 화면에서 A01로 전환 시 A01의 "이야기 시작" 버튼이 여전히 보이는지(가려지지 않는지)
   확인.
4. `npx vitest run`(병합 후 전체 통과 기대), `npx tsc -b`, `npx playwright test`
   (`no-change-and-scenario.spec.ts`, `multi-recipient-flow.spec.ts` 포함) 재실행.

**위험**: `shared/noChangeEngine.ts` 도메인 키가 늘어나 이 파일에 의존하는 기존 e2e
문구 검증(admin 쪽 도메인 라벨 등)이 깨질 수 있다 — 병합 후 전체 e2e 재실행 필수.

---

### 제안 2 — 관리자 응답이 요양보호사 화면에 돌아오게 하기 (최소 노출)

**무엇**: 새 기능을 만들지 않고, 이미 DB에 저장돼 있는 `review_status`/`review_note`/
`reviewed_at`를 요양보호사의 기존 "내가 남긴 돌봄기록" 상세 화면(`CareApp.tsx`의
`historyDetail` 화면, `CareReportDetail` 타입에 이미 필드 존재)에 그대로 보여준다.

- 백엔드 변경 없음(데이터는 이미 있음). `api/care/reports.ts`의 GET(id 지정) 응답에 이미
  전체 컬럼(`CARE_DETAIL_COLUMNS = '*'`)이 포함되므로 프론트만 렌더링 추가.
- `CareApp.tsx`의 `historyDetail` 렌더 블록에 review_status가 `approved`/`rejected`일 때만
  "관리자 확인: 승인/반려" + `review_note`(있으면) + `reviewed_at`을 추가로 표시. `pending`
  이면 아무 것도 추가하지 않는다(과잉 정보 금지 — "아직 검토 전"이라고 굳이 강조하지 않음,
  단 사용자가 원하면 "검토 대기 중" 한 줄 정도는 추가 가능).

**왜 지금**: 오늘 확정된 핵심 흐름 5단계("관리자 응답·조치가 현장에 돌아온다")를 만족하는
현재 유일한 구멍이면서, 필요한 데이터가 이미 전부 저장돼 있어 프론트 렌더링 한 곳만 추가하면
된다 — 새 컬럼, 새 API, 새 상태 없음.

**확인 기준**:
1. 관리자 화면에서 임의 보고 1건을 반려(반려 사유 포함)로 처리.
2. 같은 요양보호사 계정으로 로그인 → "내가 남긴 돌봄기록" → 해당 보고 열기 → 반려 사유가
   보이는지 확인. 승인 케이스도 동일하게 확인.
3. `pending`(아직 검토 안 함) 보고를 열었을 때 오류나 빈 값 대신 자연스러운 기본 상태로
   보이는지 확인(예: 아무 것도 안 보이거나 "검토 대기 중"만 보임 — "반려됨"처럼 잘못
   표시되면 실패).
4. 데모 모드와 실제 모드 모두에서 확인(데이터 소스만 다를 뿐 필드는 동일).

**위험**: 낮음(순수 읽기 전용 표시 추가). 다만 `review_note`에 개인정보가 실수로 입력되지
않도록 안내 문구를 관리자 쪽에 유지해야 한다(기존 정책 — 이번 제안이 새로 만드는 위험은
아님).

---

### 제안 3 — 관리자 "열람 시각"과 "반영(승인/반려) 시각" 분리

**무엇**: `reports` 테이블에 nullable `admin_first_viewed_at timestamptz` 컬럼을 멱등
`alter table add column if not exists`로 추가하고, 관리자가 보고 상세를 처음 여는 시점에
(이미 값이 없을 때만) 1회 기록한다. 기존 `reviewed_at`(승인/반려 시각)은 그대로 둔다.

**왜 지금**: Master의 로그 요구사항 "전달·열람과 실제 돌봄 반영도 구분한다"를 만족하는
컬럼이 현재 하나도 없다 — `reviewed_at`만으로는 "언제 열어봤는지"와 "언제 실제로
승인/반려했는지"를 구분할 수 없다(관리자가 보고서를 열어만 보고 한참 뒤에 처리했을 수
있음). 컬럼 1개 추가로 해결되는 가장 작은 범위다.

**확인 기준**:
1. 관리자가 `pending` 보고를 처음 열면 `admin_first_viewed_at`이 채워지는지(DB 또는 데모
   localStorage 확인).
2. 같은 보고를 다시 열어도 값이 갱신되지 않는지(최초 1회만 기록).
3. 승인/반려 후에도 `admin_first_viewed_at`은 유지되고 `reviewed_at`만 새로 채워지는지.
4. 기존 통계(`shared/statsCalc.ts`)를 건드리지 않았는지 — 이번 제안은 저장만 하고 집계
   지표를 새로 만들지 않는다(요청 시 별도 작업으로).

**위험**: 낮음(추가 전용 컬럼, 기존 로직 미변경). Supabase 실DB에는 수동 `alter table`
적용이 필요하다(기존 절차와 동일, `docs/PILOT_LAUNCH_CHECKLIST.md`).

---

### 제안하지 않은 것 (이번 범위에서 의도적으로 제외)

- **AI 질문 음성 출력 / 요약 낭독(TTS)**: 코드가 전혀 없고, 이 환경에서 실제 음성 API
  연결·실기기 검증이 불가능하다. 문구·캐릭터만으로 "음성 대화 완성"처럼 보이게 만들지
  않기 위해 이번 최소 구현에서 제외했다 — 별도 세션에서 실제 TTS 공급자를 정하고
  실기기로 검증하며 진행해야 한다.
- **캐릭터/아바타**: 이미지 생성·애니메이션 구현은 이번에 하지 않는다(요청에도 명시).
- **음성 기반 전송 동의**: 현재 명시적 버튼 클릭만 동의로 인정하는 안전한 상태를 유지한다
  — 음성 동의를 추가하려면 "침묵/일반 대화의 네/AI 음성을 동의로 오인하지 않는" 조건을
  실기기에서 검증해야 하므로 별도 작업으로 분리한다.
- **API 공급자 변경, 유료 비교 실험**: 요청대로 별도 작업으로 남긴다.

### 남아 있는 불확실성 (Codex/다음 세션이 확인할 것)
- `ai365-care-dream-handoff-4a85b3`의 미커밋 변경(D6 라벨, `careReportAi.ts` 프롬프트
  수정)이 제안 1의 병합과 충돌하지 않는지 — 그 워크트리 세션이 커밋을 마친 뒤 다시 확인
  필요.
- 실제 Vercel 배포가 어느 커밋을 서비스 중인지 이 환경에서 확인 불가 — 제안 1을 병합해도
  실제 배포까지 이어지는지는 별도 확인 필요.
- 이 워크트리와 handoff 워크트리의 MASTER_CONTEXT.md/AGENTS.md를 하나로 합칠지는 사용자
  결정 대기 중(이번 세션은 의도적으로 분리해 충돌을 피했다).


