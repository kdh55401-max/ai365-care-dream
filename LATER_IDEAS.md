# LATER_IDEAS — 이번 실증 범위 밖 아이디어

이번 파일럿(`/care`, `/admin`, 2026-09-07~09-18)에는 의도적으로 넣지 않은 것들.
사양서 12번 항목에서 명시적으로 제외한 기능은 여기 다시 적지 않는다.

- **Supabase Realtime 기반 즉시 갱신**: 지금은 관리자 대시보드가 8초 폴링으로
  새로고침 없이 갱신된다. 참여자/데이터가 늘어나면 `postgres_changes` 구독 +
  커스텀 JWT 클레임 기반 RLS 정책으로 전환해 폴링 부하를 없앨 수 있다.
- **Playwright 등 자동 E2E 테스트**: 이번에는 순수 로직(통계 집계, CSV) 단위테스트만
  작성했다. 브라우저 자동화(preview 도구)로 로그인 화면까지는 수동 확인했지만,
  Supabase 실계정 없이는 전체 흐름(로그인→AI턴→제출→관리자평가)을 CI에서 자동
  검증할 수 없었다. 실제 Supabase 프로젝트가 만들어진 뒤 Playwright 모바일
  프로파일로 골든 패스 E2E를 추가하면 좋다.
- **참여자별 관리자 계정 분리(다중 관리자)**: 지금은 관리자 비밀번호 1개를
  공유한다. 관리자가 여러 명이면 개별 계정 + `admin_audit_log`의 `actor` 컬럼
  분리가 필요하다.
- **PIN 무차별 대입 방어(레이트리밋)**: 9명·4자리 PIN 규모의 단기 실증이라
  별도 rate limit을 넣지 않았다. 실증 확대 시 IP/코드별 시도 횟수 제한 추가.
- **참여자-수급자 고정 매핑**: 지금은 로그인한 요양보호사가 매번 활성 수급자
  목록 중에서 고른다. 실제 배정이 고정적이라면 `participant_recipient_map`
  테이블을 추가해 선택 단계를 없앨 수 있다(60초 목표에 더 유리).
- **관리자 열람(view) 감사 로그 세분화**: 지금은 보고 상세를 열 때만 `view_report`
  감사 로그를 남긴다(목록 조회는 남기지 않음). 더 엄격한 감사가 필요하면 목록
  조회도 요약해서 남기는 방식을 고려.
- **음성 인식 언어/방언 튜닝, 오프라인 녹음 후 업로드**: 브라우저 Web Speech
  API(ko-KR)만 사용한다. 네트워크가 불안정한 현장을 고려한 오프라인 녹음 큐잉은
  다음 단계.
- **관리자 알림(신규 제출 푸시/카카오 알림톡)**: 지금은 관리자가 대시보드를
  직접 확인해야 한다.
- **Supabase Realtime**: 지금은 3초 폴링이다. 참여자가 늘어나면 Realtime
  구독 + RLS 정책으로 전환해 폴링 부하와 지연을 없앨 수 있다.
- **투자자 피칭 화면의 "중간집계/최종 실증 결과" 상태 서버 저장**: 지금은
  관리자가 화면에서 고르는 값이 새로고침하면 초기화된다(브라우저 상태로만
  유지). 실증 종료 시점에 정말 "최종"으로 고정하려면 관리자 설정을 저장할
  작은 테이블이 필요하다.
- **지표 정의 툴팁을 모달/문서로 확장**: 지금은 카드별 작은 팝오버다. 투자자
  피칭 준비용으로 지표 산출근거를 모아놓은 별도 페이지가 있으면 더 좋다.
- **WebKit(Safari) 기반 E2E**: 이번 Playwright 설정은 이 개발 환경에 이미
  있는 Chromium 엔진만 사용했다(WebKit 바이너리 추가 다운로드가 필요해
  생략). 아이폰 실기기 검증은 문서화된 수동 체크리스트로 대체했다.
- **표준상황 연습의 전문가 2인 적절성 평가**: 자동으로 채점 가능한 항목
  (필수정보 확인률, 금지어 등장 여부)만 구현했다. 사람이 직접 평가하는
  "전문가 적절성"은 UI만 "검증 전"으로 표시해 두었고, 실제 평가 입력 화면은
  이번 범위에 넣지 않았다(임의 수치를 만들지 않기 위해 의도적으로 보류).
