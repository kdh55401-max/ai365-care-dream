# AGENTS.md — AI365 CARE DREAM 공통 작업 원칙

이 문서는 Claude Code와 Codex가 이 저장소에서 작업할 때 공통으로 지키는 규칙이다. 도구별
세부 진입 안내는 §5를 본다. 최신 목표·구현 상태는 [MASTER_CONTEXT.md](MASTER_CONTEXT.md),
변경 이력은 [CHANGE_LOG.md](CHANGE_LOG.md)를 먼저 읽는다 — 이 파일들과 내용이 겹치면 그쪽이
최신이다.

## 1. 먼저 읽을 것 (순서)

1. [MASTER_CONTEXT.md](MASTER_CONTEXT.md) — 목표, 핵심 흐름, 현재 구현 상태, 미확인 사항.
2. [CHANGE_LOG.md](CHANGE_LOG.md) — 최근 변경이 왜, 무엇을, 어떻게 검증됐는지.
3. 이번 작업이 Codex 검증을 겸한다면 [HANDOFF_TO_CODEX.md](HANDOFF_TO_CODEX.md).
4. 필요한 범위에서만 [docs/CARE_GUIDE.md](docs/CARE_GUIDE.md),
   [docs/ADMIN_GUIDE.md](docs/ADMIN_GUIDE.md),
   [docs/PILOT_LAUNCH_CHECKLIST.md](docs/PILOT_LAUNCH_CHECKLIST.md), [LATER_IDEAS.md](LATER_IDEAS.md).

과거 대화나 다른 AI 세션의 내용이 자동으로 공유된다고 가정하지 않는다 — 위 문서에 없는 결정은
사용자에게 확인한다.

## 2. 절대 지켜야 할 핵심 가치 (수정 시 사용자 확인 필요)

"요양보호사가 대상 어르신의 돌봄을 편하게 이야기하면 → AI가 필요한 것만 확인해 기록으로
정리하고 → 관리자가 검토해 활용한다." 자세한 핵심 흐름은 MASTER_CONTEXT.md §3.

- 시작 버튼 하나로 핵심 대화를 시작하는 방식을 유지한다. 여러 화면 이동·불필요한 입력을
  늘리지 않는다.
- "특이사항 없음"도 유효한 기록이다. 정상 응답을 위험·상태 변화로 집계하지 않는다.
  `same_as_usual` / `changed` / `not_observed` / `uncertain`을 구분한다(MASTER_CONTEXT.md §5).
- 요양보호사 식별자는 C01~C09, 수급자는 A 계열. C↔A 관계는 별도 매핑 테이블
  (`caregiver_assignments`)로 관리하고 ID 자체에 인코딩하지 않는다.
- 응급 연락(119/센터 전화)과 기록 저장 기능의 경계를 분리 유지한다. AI 응답을 의료 진단이나
  실제 연락 완료처럼 표시하지 않는다.
- 실제 돌봄기록(운영 데이터)과 데모(`?demo=1`)·시나리오 연습(`/care/scenario`) 데이터를 절대
  같은 집계에 섞지 않는다.

## 3. 작업 범위와 태도

- 기존에 잘 동작하는 기능을 보존한다. 요청받지 않은 리팩터링·전면 재작성·새 기능 추가를
  하지 않는다. 사업 방향을 바꾸는 제안은 구현하지 말고 별도로 사용자에게 제시한다.
- 다른 진행 중인 워크스트림(예: 확장형 MVP `src/care/CareWorkspace.tsx`, TEAM/COMMUNITY,
  생활안전스캐너)은 이 파일럿(`/care`, `/admin`)과 분리되어 있다 — 라우팅되지 않는 코드를
  이 작업 중 임의로 수정·삭제하지 않는다.
- 수급자 실명·건강 상세, 참여자 실명-코드 대응표, 비밀번호, API 키를 문서나 커밋에 남기지
  않는다.
- 실제 운영 기록을 시험 목적으로 임의 변경하지 않는다. 테스트 데이터가 필요하면 데모 모드나
  소프트 삭제로 분리한다.
- 사실, 사용자 보고, 해석, 제안, 미확인 사항을 구분해서 보고한다. 실행하지 않은 테스트·확인
  하지 않은 화면을 완료했다고 말하지 않는다.

## 4. 검증 기준선

코드를 바꾼 뒤에는 최소한 다음을 실행하고 결과를 보고에 남긴다(가능한 범위에서):

```bash
npm install
npx tsc -b
npx vitest run
npx oxlint
npx vite build
```

UI에 영향이 있으면 데모 모드(`?demo=1`)로 실제 클릭 흐름을 확인한다. `npx playwright test`
(mobile-390/360)는 가능하면 실행하되, 이 개발 환경에는 Gemini/Supabase 자격증명이 없어 실
API 경로는 검증할 수 없다는 점을 항상 보고에 명시한다.

## 5. 도구별 진입 지침

### Claude Code
- 세부 규칙은 [CLAUDE.md](CLAUDE.md) 참고(이 파일을 대체하지 않고 보완한다).
- 주 구현자 역할: 기능 개발, 버그 수정, 개선안 반영. 작업 후 코드가 바뀌었다면
  [CHANGE_LOG.md](CHANGE_LOG.md)에 항목을 추가하고, Codex 검증이 필요하면
  [HANDOFF_TO_CODEX.md](HANDOFF_TO_CODEX.md)를 갱신한다(§6 형식).
- 긴 원본 자료(PDF/PPT 등)를 매번 재분석하지 않는다 — 압축 문서를 기준으로 삼는다.

### Codex
- 검증 담당 역할: 실제 코드·동작·화면·로그를 점검해 기능 오류, 데이터 정합성, UI/UX 개선을
  제시한다.
- 시작 전에 [HANDOFF_TO_CODEX.md](HANDOFF_TO_CODEX.md)의 "기준 브랜치·커밋"을 확인해 Claude와
  동일한 버전을 보고 있는지 맞춘다 — 서로 다른 브랜치/워크트리를 보고 결론 내지 않는다.
- 검토 결과는 각 항목을 반영해야 할 이유(근거: 파일·함수·재현 절차·로그)와 함께 제시한다.
  사업 방향 자체를 바꾸는 제안은 기능 수정과 분리해 표시한다.
- Claude와 같은 작업 폴더에서 동시에 같은 파일을 수정하지 않는다. 별도 브랜치/워크트리를
  쓴다면 검토 대상 커밋 SHA를 명시한다.

#### 변경 중심 검증과 간결한 인계
- 필수 지침을 지키되, 이미 읽고 변경되지 않은 설명·인계 문서는 다시 전문으로 읽지 않는다.
  최신 HANDOFF_TO_CODEX 항목·커밋·배포 대응·통과 기준부터 확인하고 diff → 관련 함수·호출부·
  테스트 순서로 범위를 좁힌다. 수정본이 없으면 동일 버전을 반복 시험하지 않고 미수정으로 보고한다.
- 독립적인 읽기·검색은 묶고 필요한 구간·오류만 확인한다. 의존 경로 확장과 스크린샷·시각화는
  구체적인 검증 필요가 있을 때만 사용한다. 접근 실패를 같은 방식으로 반복하지 않는다.
- 변경 영향에 맞는 실제 검증과 기존 필수 테스트·빌드 게이트를 유지한다. 코드/데모/실제 API/
  운영 DB/실기기를 구분하고 미실행은 미검증으로 남긴다. 추론 수준·모델 설정을 임의 낮추거나
  확인하지 않은 토큰 절감률을 주장하지 않는다.
- 최종 보고는 기본 15줄 안팎으로 대상 버전·환경 → 판정 → 중요도·재현·영향 → 수행 검증 →
  미검증 → Claude의 다음 수정 순서로 쓴다. 상세 근거는 기존 인계/검토 문서 한 곳에만 남기고
  보고 파일과 내용을 중복 생성하지 않는다. 같은 재검증은 현재 세션에서 이어간다.
- 현재 우선 검증 범위는 [HANDOFF_TO_CODEX.md](HANDOFF_TO_CODEX.md)의 최신 항목을 따른다(이
  파일 자체에 특정 버그 목록을 고정해두지 않는다 — 인계 문서가 항상 최신이다). 별도 지시
  없이 디자인·지표 개편을 임의로 확장하지 않는다.

## 6. Claude → Codex 인계 형식 (기능 수정 후 필수)

`HANDOFF_TO_CODEX.md`에 다음을 남긴다:

- 이번 작업의 목적과 기대 동작
- 기준 브랜치·커밋 또는 아직 커밋하지 않은 변경 범위
- 수정 파일과 핵심 변경 내용
- 직접 수행한 테스트와 결과
- 확인할 화면·재현 순서·시험 데이터 조건
- 남아 있는 불확실성과 Codex에게 요청하는 점검 항목

Codex 검토 결과를 받으면 각 항목을 "반영" / "대안 반영" / "보류"로 구분하고 이유를 기록한다.
버그 수정 후에는 같은 재현 절차로 재검증할 수 있도록 결과를 인계한다.
