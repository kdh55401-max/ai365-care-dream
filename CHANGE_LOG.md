# CHANGE_LOG — 60초 AI 돌봄보고 (CARE REPORT) 실증 파일럿

이 로그는 `claude/care-report-pilot-mvp-a58535` 브랜치에서 진행한 실증 파일럿 구현만
기록한다. 기존 확장형 MVP(TEAM/CARE/COMMUNITY, 생활안전스캐너 등)는 손대지 않았다.

## 2026-09-04 (2차) — 데모 모드, 투자자 대시보드, "특이사항 없음" 대응 흐름

### 배경
Supabase·Gemini 실계정 없이도 사용자가 실제 브라우저에서 전체 흐름을
직접 시연·검증할 수 있어야 한다는 요구에 따라, 외부 서비스에 전혀
의존하지 않는 **데모 모드**를 추가했다. 동시에 관리자 화면을 투자자
피칭이 가능한 실증 대시보드로 확장하고, 실제 현장에서 가장 흔할 "평소와
비슷했어요"(특이사항 없음) 상황을 강제 유도 없이 구조화하는 흐름을 넣었다.

### 만든 것
- **데모 모드** (`?demo=1`) — `src/pilot/demo/*`. Supabase/Gemini 없이
  localStorage + BroadcastChannel만으로 `/care?demo=1`, `/admin?demo=1`,
  `/admin/presentation?demo=1`, `/care/scenario?demo=1` 전체 흐름이 그대로
  작동한다. 참여자 PIN은 전부 `1234`, 관리자 비밀번호는 `demo1234`(화면에
  안내 표시). AI 질문·구조화는 규칙 기반 엔진(`demoAiEngine.ts`)이 대신하며,
  모든 화면에 "DEMO DATA · 실제 실증 결과가 아닙니다" 배지를 고정 표시한다.
  "데모 초기화" 버튼으로 언제든 리셋 가능. 실제 API 코드(`api/*`, DB 스키마)는
  전혀 건드리지 않고 완전히 분리된 경로로 구현했다.
- **공용 로직 재배치** — `api/_lib/statsCalc.ts`, `csv.ts`를 저장소 루트의
  `shared/`로 옮겨 서버(api/, tsconfig.api.json)와 브라우저(src/,
  tsconfig.app.json) 양쪽에서 동일한 통계·CSV 로직을 그대로 재사용한다
  (데모 모드가 실제 서버와 동일한 계산식을 쓰도록 보장하기 위함).
- **관리자 실증 대시보드 확장** — `src/pilot/admin/AdminApp.tsx`,
  `charts.tsx`. 실증 헤더(기관명, 기간, LIVE 배지), 6개 핵심 KPI 카드(분자/
  분모 항상 표시), 순수 SVG 막대·선 그래프(전후 비교, 일자별 누적), 참여자별
  표+히트맵, 실시간 돌봄보고 피드(3초 폴링), 전후 비교 상세, 투자자 피칭
  화면(`/admin/presentation`, 민감정보 제외), 지표 정의 툴팁(ⓘ). 실제
  데이터가 없을 때는 카드에 `측정 전`/`0 / 9명`처럼 표시하고 그래프에만
  "제출되면 표시됩니다" 안내를 띄운다(사양서 14번 요구사항).
- **"특이사항 없음" 대응 흐름** — `shared/noChangeEngine.ts`(규칙 기반,
  Gemini 불필요), `src/pilot/care/CareApp.tsx`. 돌봄보고 시작 후 "평소와
  다른 점 있음 / 평소와 비슷함 / 확인 필요 / 직접 말하기" 3+1 선택지를
  먼저 제시하고, "특이사항 없음" 계열 발화는 자동 감지해 같은 흐름으로
  연결한다. 이 흐름은 고정 질문 최대 2개(유도질문 없음)로 9개 관찰영역
  (식사·수분/이동·낙상/배설/인지·의사소통/정서·행동/통증·호흡/수면/피부·
  위생/복약 관찰)을 `same_as_usual/changed/not_observed/uncertain` 중 하나로
  분류하고, 언급되지 않은 영역은 절대 "정상"으로 바꾸지 않는다.
- **표준상황 연습** (`/care/scenario`) — 고정된 가상 상황 2개, `report_source
  ='scenario'`로 실제 현장보고와 완전히 분리 저장·집계(`computeScenarioStats`).
  필수정보 확인률·사실 생성 여부는 시나리오별 정답 키워드로 자동 채점한다.
- **DB 스키마 확장** — `db/schema.sql`에 특이사항 없음 흐름 필드(13개) 추가,
  기존 테이블에도 안전하게 적용되도록 `alter table ... add column if not
  exists` 멱등 블록을 포함했다.
- **Playwright E2E** — `playwright.config.ts`, `e2e/*.spec.ts`. 데모 모드
  기준 골든 패스(요양보호사 제출→관리자 확인→평가→지표 갱신), 권한 격리,
  새로고침 후 데이터 유지, 특이사항 없음/표준상황 분리를 모바일 뷰포트
  (390×844, 360×800)에서 검증한다.

### 실제 브라우저 시연 중 발견하고 고친 버그 (중요)
"코드가 있다"와 "실제로 작동한다"는 다르다는 지적에 따라, 이번에는 로컬
브라우저에서 데모 모드 전체 흐름을 직접 클릭해 검증했고, 그 과정에서
실제로 3개의 버그를 발견해 고쳤다(자동 테스트만으로는 못 잡았을 것들):
1. 제출 직후 "센터에 보고되었습니다." 화면이 뜨자마자 홈으로 튕기던 버그
   (`loadHome`이 화면 상태까지 덮어씀) → `navigateToHome` 플래그로 분리.
2. "평소와 비슷했어요"를 눌러도 자유 입력 화면으로 새는 버그 → 바로 고정
   질문으로 연결하도록 수정.
3. 모든 분수 표시(`OO건 중 OO건, N%`)의 분자/분모 순서가 사양서 예시와
   반대였던 버그 → 표기 규칙을 통일해 수정.

### 왜 이렇게 했는가
- **데모 모드에 서버를 두지 않은 이유**: Vercel 서버리스 함수는 요청마다
  상태가 사라지므로, 외부 계정 없이 "즉시 시연 가능"하려면 순수 브라우저
  저장(localStorage)만으로 완결되는 구조가 유일하게 확실한 선택이었다.
  대신 같은 브라우저의 다른 탭끼리만 실시간 동기화되고, 시크릿창과는
  동기화되지 않는다는 한계를 화면·문서에 명시했다.
- **특이사항 없음 분류를 규칙 기반으로 만든 이유**: 질문 문구가 사양서에서
  이미 고정 문장으로 지정돼 있어 AI가 재량으로 바꿀 이유가 없고, 오히려
  규칙 기반으로 만들면 Gemini 키가 없어도(그리고 데모 모드에서도) 100%
  동일하게 동작해 이 흐름 전체가 외부 의존성 없이 신뢰 가능해진다.

## 2026-09-04 — 실증 파일럿 최초 구현

### 배경
기존 확장형 MVP 개발을 일단 중단하고, 단일 가설 검증을 위한 초경량 실증 MVP를
별도 브랜치에 새로 구현했다. 가설: "요양보호사가 방문 후 관찰한 내용을 말하면 AI가
부족한 정보를 되물어, 센터가 추가 질문 없이 바로 판단할 수 있는 구조화된 돌봄보고를
만들 수 있다."

### 만든 것
- **DB 스키마** ([db/schema.sql](db/schema.sql)) — Supabase(Postgres). `participants`,
  `recipients`, `reports`, `admin_audit_log` 4개 테이블. RLS 활성화, 접근은 서버의
  Service Role 키로만 허용(브라우저 직접 접근 불가).
- **서버 API** (`api/care/*`, `api/admin/*`, `api/_lib/*`) — Vercel Node 서버리스
  함수. 참여자 PIN 로그인/세션(JWT httpOnly 쿠키, 14일), 관리자 비밀번호
  로그인(bcrypt 해시 비교), 돌봄보고 작성·조회·제출, AI 질문/구조화 턴
  (`api/_lib/careReportAi.ts`, Gemini `gemini-3.6-flash`, JSON 스키마 강제, 최대
  3회 질문 서버 강제), 관리자 2단계 블라인드 평가, 통계 집계
  (`api/_lib/statsCalc.ts`), CSV 내보내기(요약/전체), 참여자 PIN 초기화, 보고
  소프트 삭제.
- **/care 화면** ([src/pilot/care/CareApp.tsx](src/pilot/care/CareApp.tsx)) — 참여자
  코드+PIN 로그인 → 홈(오늘 상태, 수급자 선택, 기본/추가 보고 버튼, 최근 목록) →
  음성/텍스트 입력(둘 다 항상 노출) → AI 추가 질문(최대 3회, 한 번에 하나) →
  최종 보고문 확인·수정 → 제출 → "센터에 보고되었습니다." 화면. 기기 내
  localStorage + 서버 draft 이중 저장으로 새로고침/네트워크 오류 시 복구.
- **/admin 화면** ([src/pilot/admin/AdminApp.tsx](src/pilot/admin/AdminApp.tsx)) —
  비밀번호 로그인 → 대시보드(핵심지표, 목표 진행률, 전후 비교, 참여현황표, 8초
  폴링 갱신) / 보고 목록·상세(1단계 원문 블라인드 평가 → 저장 후 2단계 AI 보고
  평가 공개, 재평가 시 이력 보존) / 참여자 관리(PIN 초기화) / CSV 다운로드.
- **라우팅**: `src/main.tsx`에 `/care`, `/admin` 두 경로를 추가하고 이 브랜치에서만
  `/care`를 파일럿 화면에 연결했다. 기존 `src/care/CareWorkspace.tsx` 파일은 삭제·
  수정하지 않고 그대로 두었다(더 이상 어떤 경로에서도 불러오지 않을 뿐). 다른
  브랜치/배포본은 영향을 받지 않는다.
- **테스트**: `vitest` 도입, 순수 집계 로직(`statsCalc.ts`, `csv.ts`) 단위테스트
  15개 작성 및 통과. `npm run build`(tsc + vite), `npm run lint`(oxlint) 통과.

### 왜 이렇게 했는가 (주요 설계 판단)
- **실시간 갱신 = 폴링(8초)**: Supabase Realtime/커스텀 JWT 기반 RLS 대신, 참여자
  9명·평가 90여 건 규모의 단기 실증에서는 8초 폴링으로도 "새로고침 없이 반영"
  요구를 충분히 만족하면서 구현 복잡도와 실패 지점을 크게 줄일 수 있다고 판단했다.
- **DB 접근은 항상 서버 경유**: Supabase anon 키를 브라우저에 두지 않고, 모든
  읽기/쓰기를 Vercel 서버리스 함수(Service Role 키)로만 수행한다. RLS는 활성화만
  해두고 정책은 추가하지 않아 기본적으로 모든 직접 접근을 차단한다.
- **"수정 없이 사용된 보고 비율"은 자동 비교로 계산**: 관리자가 별도로 "수정 필요
  정도"를 입력하지 않고, `ai_generated_report`와 `caregiver_final_report`를 서버가
  직접 비교해 집계한다. 관리자 평가 항목 수를 늘리지 않기 위한 선택.
- **AI 질문 3회 제한은 서버가 이중으로 강제**: 프롬프트에 규칙을 명시하는 것과
  별개로, 서버가 4번째 턴부터는 무조건 `needFollowup=false`로 마무리하고, AI가
  보고문 생성에 실패해도 원문 그대로를 담은 대체 보고문(fallback)을 만들어 흐름이
  끊기지 않게 했다.
