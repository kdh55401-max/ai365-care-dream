# CHANGE_LOG — 60초 AI 돌봄보고 (CARE REPORT) 실증 파일럿

이 로그는 `claude/care-report-pilot-mvp-a58535` 브랜치에서 진행한 실증 파일럿 구현만
기록한다. 기존 확장형 MVP(TEAM/CARE/COMMUNITY, 생활안전스캐너 등)는 손대지 않았다.

## 2026-09-10 (2차) — CD-01/CD-02 통합, 관리자 응답을 요양보호사 기록에 연결

Master 승인 하에 바로 위 분석(1차) 항목의 "권장 최소 구현" 2건을 구현했다. 커밋
`13e7801`(CD-01/CD-02 병합, 새 코드 없음)에 이어 `1d9373f`(관리자 응답 연결 구현)에서
검증까지 마쳤다 — 이 두 커밋이 이번 회차의 전체 변경분이다.

### 1) CD-01/CD-02 통합
`ai365-care-dream-handoff-4a85b3`의 커밋 `2425a4c`+`bc7e36d`(이미 검증됨)를 재작성하지
않고 그대로 병합했다 — 관찰영역(식사/수분/이동/낙상) 세분화, 미응답 항목을 "없음"으로
단정하지 않는 문구, not_observed/uncertain 문구 분리, 참여자+수급자 단위 일일보고 완료
판정. 데모 브라우저로 직접 재현: "식사, 이동, 배설 모두 평소와 같아요. 수분 섭취는
관찰하지 못했어요."를 입력하면 수분만 별도로 "관찰하지 못했다"로 남고 식사·이동·배설과
섞이지 않으며, 조치/센터확인은 "미응답"/"아직 검토되지 않음"으로 남는다. C01이 A02에
제출해도 A01은 시작 버튼이 그대로 남는다.

### 2) 관리자 응답을 "내가 남긴 돌봄기록"에 연결
`review_note`가 지금까지 관리자 화면에만 보이는 반려 사유였다는 점을 코드로 확인한 뒤,
새 컬럼 `review_note_visible_to_caregiver`(기본값 false, 관리자가 매번 명시적으로
선택해야 true)를 추가해 공개 여부를 명시적으로 구분했다 — 이 컬럼이 생기기 전 메모는
전부 false이므로 소급 공개되지 않는다. 요양보호사의 "내가 남긴 돌봄기록" 목록에는
"관리자 검토 대기/관리자 확인: 승인됨/반려됨" 한 줄을, 상세 화면에는 승인·반려 상태 +
(공개된 경우에만) 응답 내용과 응답 시각을 보여준다 — `reviewed_at`은 "이 검토가 기록된
시각"으로만 쓰고 열람·현장반영 시각으로 대체하지 않았다. 부수적으로
`demoCareRepo.getReport`에 참여자 소유권 검사가 빠져 있던 것을 발견해 실서버(API)와
같은 기준으로 맞췄다(다른 참여자 보고 id로는 조회 불가).

### 검증
`npx vitest run`(113/113, 신규 4건: 공개 여부 게이팅 2건 + 소유권 격리 1건 + 기존 1건
확장), `npx tsc -b`(0 오류), `npx oxlint`(기존 경고 2건만), `npx vite build` 통과.
`npx playwright test`(mobile-390/360, 42개) — 41/42 통과, 1건(no-change-and-scenario
mobile-390)은 단독 재실행 시 통과해 부하로 인한 기존 플레이크로 확인(이번 변경과 무관).
데모 브라우저로 승인/반려·공개 체크/미체크 4가지 조합을 모두 클릭해 확인했다: 미체크
시 요양보호사 화면에 상태만 보이고 내용은 안 보임, 체크 시 내용+시각까지 보임. 실제
Gemini/Supabase, 실기기는 이 환경에 자격증명·하드웨어가 없어 미검증.

### 제외 범위 (요청대로)
`admin_first_viewed_at` 신규 컬럼, TTS/Live API/아바타/음성 전송 동의, 대규모
화면·DB 재작성, 새 분석 시스템, Gemini 비교 실험.

## 2026-09-10 — 음성 중심 돌봄 UX 18개 항목 분석 (코드 변경 없음)

### 배경
Master가 전달한 음성 대화 UX/UI 설계 18개 항목을 현재 구현과 비교해 달라는 요청. 이번
회차는 명시적으로 "코드부터 수정하지 말고 최소 구현 범위를 제안"하는 분석 전용 작업이다.

### 확인한 것 (코드 변경 없음)
- **브랜치/배포 갈림 확인**: `master`(`0c8410a`, 이 워크트리와 동일)와 별도로
  `ai365-care-dream-handoff-4a85b3` 워크트리에 이미 검증됐지만 **미병합·미배포**인 수정
  2건이 있음을 읽기 전용으로 확인했다 — CD-01(관찰영역 세분화: `meal_hydration`/
  `mobility_fall`을 4개로 분리, "미응답"을 "조치 없음"으로 단정하지 않기)과 CD-02(수급자별
  일일보고 완료 판정 — 현재 master는 참여자 단위로만 판정해 다른 대상자 제출이 오늘 대상자의
  시작 버튼을 가림). 두 수정 모두 `npx vitest run`(107~111건)·`npx playwright test` 검증까지
  마친 상태로 그 워크트리에 남아 있다 — 이번 세션은 병합하지 않고 병합을 "즉시 실행 가능한
  최소 구현"으로만 제안한다(아래 HANDOFF_TO_CODEX.md).
- **음성 출력(TTS) 코드 부재 확인**: `speechSynthesis`/`SpeechSynthesisUtterance`/TTS 관련
  코드가 저장소 전체에 전혀 없음을 grep으로 확인 — AI 질문 음성 출력, 요약 낭독은 완전
  미구현이다. 음성 인식(STT, `useContinuousVoice.ts`, Web Speech API)만 구현돼 있다 — 이
  둘을 하나로 뭉쳐 "음성 대화가 됐다"고 보고하지 않는다.
- **캐릭터/아바타 UI 부재 확인**: 관련 코드 전혀 없음(브랜드 톤·색상·문구만 존재).
- **관리자→현장 역방향 연결 부재 확인**: `CareApp.tsx` 어디에서도 `review_status`/
  `review_note`/`reviewed_at`을 읽지 않는다 — 관리자가 승인/반려해도 요양보호사 화면에는
  전혀 나타나지 않는다. DB에는 이미 값이 저장되므로, 기존 "내가 남긴 돌봄기록" 상세
  화면(`historyDetail`)에 그대로 노출하는 것이 가장 작은 구현으로 판단된다.
- **전송 동의 안전조건**: 현재 제출은 `reportReview` 화면의 명시적 버튼 클릭
  ("이대로 센터에 보내기")으로만 가능하고 음성 기반 동의 로직 자체가 없어, "침묵·일반
  대화의 '네'를 동의로 처리하지 않는다"는 조건은 기능 부재로 인해 저절로 지켜지고 있다.
- **상태 구분**: 듣는 중(`voice.state==='listening'`)/처리 중(`loading` +
  `screen==='question' && !currentQuestion`)/질문 중(`currentQuestion` 있음)/확인
  대기(`reportReview`)/전송 중(제출 버튼 로딩)/전송 완료(`screen==='submitted'`, 실패 시
  절대 도달하지 않음 — 이전 세션에 분리 확인됨)는 화면상 이미 전부 구분되나, 음성/오디오
  신호는 어디에도 없다(화면을 안 보면 상태를 알 수 없음).
- **로그 측정 가능성**: 질문 횟수는 `followup_questions.length`로 측정 가능. 이탈 지점은
  `status='draft'` + `followup_questions.length`로 대략 추정 가능하나 전용 이벤트 로그는
  없음. 관리자 응답 시간은 `reviewed_at`으로 측정 가능하나 "열람 시각"과 "실제 조치 시각"이
  분리돼 있지 않다(`viewed_at` 류 컬럼 없음) — 열람과 반영을 구분하려면 컬럼 추가가 필요하다.

### 산출물 (이번 세션에서 새로 만들거나 갱신한 문서만)
- `MASTER_CONTEXT.md`(이 워크트리, 신규) — 오늘 확정된 음성 UX 설계 원칙만 압축. 정본은
  `ai365-care-dream-handoff-4a85b3`에 있다는 점을 명시했다(문서 통합은 사용자 결정 대기).
- `HANDOFF_TO_CODEX.md`(이 워크트리, 신규) — 권장 최소 구현 3건의 검증 기준·화면 확인
  절차·통과 조건.

### 검증
코드를 바꾸지 않았으므로 `tsc`/`vitest`/`playwright`를 다시 실행하지 않았다(이전 커밋
`0c8410a`에서 이미 101/101·36/36으로 확인된 상태 그대로). 이번 분석은 grep과 정적 코드
읽기, 그리고 handoff 워크트리의 커밋 메시지·diff 통계를 읽기 전용으로 확인하는 방식으로만
수행했다 — 실제 브라우저 클릭 재현은 하지 않았다(코드 변경이 없어 화면도 바뀌지 않았기 때문).

## 2026-09-08 (3차) — 요양보호사 홈 화면 재구성, 무관한 발화 처리 통합, 관리자 "미분류" 집계 신설

### 배경
요양보호사 로그인 후 화면에 대한 사용성 검토(첨부 스크린샷 기준)에 따라 홈 화면을
"누구를(오늘 돌봄 대상) → 무엇을(대화 시작 안내) → 어디를(이야기 시작)" 순으로
재구성해 달라는 요청을 받았다. 별도 브랜치(`claude/caregiver-screen-redesign-f5fc74`)에서
작업하는 동안 `master`에 병행 커밋(AI 폴백 추적 + 관리자 화면 재배치, 바로 위 항목)이
먼저 올라가 있어, 병합하며 겹치는 부분(무관한 발화 처리 프롬프트 규칙)을 정리했다.

### 만든 것
1. **홈 화면 재구성** — 오늘 돌봄 대상("A06 어르신") → 대화 시작 안내("오늘 A06
   어르신은 어떠셨어요?") → "이야기 시작"(원형, 짧은 라벨) → "글로 입력하기"(음성
   자동시작 없이 같은 대화·저장 흐름으로 진입) 순으로 배치. 미작성 안내는 작은
   pill("오늘 돌봄을 함께 기록해요")로 낮추고, "추가 상태변화 기록하기"는 오늘 기본
   보고를 실제로 저장한 뒤에만 노출해 시작 버튼과 경쟁하지 않게 했다. "최근 본인
   보고 목록" → "내가 남긴 돌봄기록"으로 개명. "60초" 문구는 목표 시간이라는 취지가
   드러나도록 수정.
2. **무관한 발화(잡담) 처리 강화** — `shared/offTopicEngine.ts` 신설: 돌봄 관련 키워드가
   조금이라도 있으면 절대 무관 판정하지 않고, 명백한 잡담 키워드(날씨/뉴스/스포츠 등)가
   있을 때만 무관으로 본다(애매한 입력은 그대로 AI에게 넘김 — 과잉 차단 방지가 목적).
   데모 엔진·실 Gemini 경로 양쪽에 결정론적 가드로 적용해 Gemini 호출 전에 되묻고,
   되물은 뒤 실제 돌봄 내용으로 답하면 그 답변을 기준으로 최종 보고문을 작성한다(원래
   무관한 발화를 다시 담지 않음). master의 병행 커밋이 이미 추가한 프롬프트 전용 규칙과
   합쳐 하나의 규칙 13으로 정리했다.
3. **관리자 집계 수정: "평소와 비슷함"에 미분류 보고가 섞이던 문제** — `shared/statsCalc.ts`의
   `bucketOf()`가 `initial_status_choice`가 null인 보고(= "이야기 시작"/"글로 입력하기"로
   상황선택 화면 자체를 거치지 않은 기본 돌봄보고)를 전부 "평소와 비슷함"으로 집계하고
   있었다 — 실제로 확인된 정상 상태와 "아직 분류되지 않음"을 구분하지 못하는 버그였다.
   `unclassified`(미분류) 버킷을 신설해 분리하고, 관리자 대시보드 "현장보고 유형"에
   5번째 카드로 노출(설명 InfoTip 포함). 과거 데이터의 실제 상태를 추정해 채우지 않는다.
4. **caregiverNote 키워드 보강** — "고민/난감/당황"을 어려움 표현으로 추가해 "어르신이
   같은 말씀을 반복하셔서 어떻게 대답할지 고민됐어요"류 발화가 요양보호사 본인의
   어려움으로 캡처되게 했다(기존에는 빠졌음).
5. **e2e 테스트 보수** — 다음 두 종류의 사전 결함을 실제로 재현·확인한 뒤 고쳤다
   (테스트를 약화하지 않고 현재 동작에 맞춰 갱신):
   - `master`의 관리자 화면 재배치(연구용 KPI가 `<details>`로 접힘, 재사용률 문구 변경)로
     깨진 admin-flow/no-change-and-scenario 테스트 — `<details>`를 열고, 바뀐 문구로
     어서션을 갱신.
   - 이전 세션에서 기본 돌봄보고가 상황선택 화면을 건너뛰도록 바뀐 뒤 갱신되지 않았던
     e2e 테스트(존재하지 않는 "직접 말하기"/"평소와 비슷했어요" 버튼을 기다리다 타임아웃,
     `/care/scenario`의 수급자 선택 드롭다운이 CurrentRecipientCard로 대체된 뒤 방치된
     `combobox` 선택자) — 커밋 11666fc(이번 세션 시작 시점의 master)에 임시 워크트리를
     만들어 같은 실패가 재현됨을 직접 확인해 사전 결함임을 검증한 뒤 현재 화면 흐름에
     맞게 재작성.

### 검증
- `npx vitest run`(101/101), `npx tsc -b`(0 오류), `npx oxlint`(무관한 기존 경고 2건만),
  `npx vite build` 모두 통과.
- `npx playwright test`(mobile-390/360, 18개 시나리오 × 2 뷰포트 = 36개) 전부 통과 —
  Playwright의 webServer 자동관리가 실행 중간에 dev 서버를 두 차례 죽이는 환경 문제를
  발견해, 직접 띄운 서버에 `PW_BASE_URL`로 고정해 재현·확인했다(앱 코드 문제 아님).
- 데모 모드 브라우저 실클릭으로 골든 패스("물을 적게 드셨어요" → 관련 후속질문 3개,
  반복 없음 → 검토 → 제출 → 관리자 확인)와 "특이사항 없어요", 무관 발화 되묻기·복구를
  확인했다. 사용자가 우려한 과잉 차단 4개 사례("날씨가 좋아서 어르신과 산책했어요",
  "더워서 어르신이 물을 적게 드셨어요", "오늘 너무 힘들었어요")를 직접 입력해 모두
  차단되지 않고 정상 처리됨을 확인했다.
- 실제 Gemini/Supabase 경로는 이 환경에 자격증명이 전혀 없어(process.env에 GEMINI_API_KEY/
  SUPABASE_URL 등 미설정, Vercel CLI 로그아웃 상태) 미검증 — 데모 규칙기반 엔진으로만
  검증했다. 실 기기 음성인식·키보드 노출 상태도 이 환경(자동화 브라우저)에서는
  검증하지 못했다.

### 배포
git 히스토리상 `master`에 직접 커밋해 온 기존 방식을 따라 병합 후 `master`에 커밋·푸시했다
(별도 배포 스크립트 없음 — GitHub 연동 배포가 있다면 push가 그대로 트리거한다). Vercel
프로젝트 자체에는 이 세션에서 접근하지 못해(로그인 정보 없음) 실제 배포 성공 여부·라이브
URL은 직접 확인하지 못했다.

## 2026-09-08 — 흰 화면 근본원인 수정, AI 폴백 추적, 관리자 화면 "센터가 확인할 돌봄" 우선순위 재배치

### 배경
9/7~9/8 실증 중 실제 요양보호사(C07)가 "흰 화면"을 반복 보고했다. 1차 조치(ErrorBoundary +
재시도 UX)는 안전장치일 뿐이었고, 이후 운영 배포 번들을 직접 검사해 그 수정조차 아직
배포되지 않았음을 확인한 뒤에야 진짜 원인을 찾았다: 후속 질문 답변 제출 시
`setCurrentQuestion(null)`이 AI 턴 응답보다 먼저 반영되는 구간 동안 `screen`은 `'question'`
그대로인데 화면이 아무것도 그리지 않아, 그 대기 시간만큼 상단 전화 버튼과 안전고지만 남고
대화 영역 전체가 비어 보였다(데모 엔진은 즉시 응답해 이 구간이 보이지 않아 그동안
재현되지 않았다). 이어서 관리자 대시보드 실사용 수치(완료율 3/2, 재사용률 0%, 완료시간
94분 등)를 근거로 지표 정의 자체를 점검해 달라는 요청을 받아 함께 처리했다.

### 만든 것
1. **흰 화면 근본원인 수정** — `question`/`historyDetail` 화면에 데이터가 아직 없는
   구간에는 로딩 안내("말씀하신 내용을 확인하고 있어요")를 명시적으로 그리도록
   분기 추가. `?e2eAiDelayMs=` 테스트 전용 파라미터로 이 버그를 재현하는 e2e
   회귀 테스트를 추가해, 수정 전 실패·수정 후 통과를 확인했다.
2. **AI 폴백(대체) 추적** — `reports.ai_fallback_used`/`ai_fallback_stage` 컬럼 추가
   (nullable, 과거 기록은 null="확인 불가" — 소급 추정하지 않음). Gemini 실패/무효
   응답으로 규칙 기반 대체가 쓰였을 때만 true로 기록하며, 하드 실패(재시도 UX로
   이어지는 경우)와는 구분한다. 관리자 화면에 "AI 정상 처리/AI 대체 처리됨/확인 불가"
   배지와 별도 발생률 지표로 노출.
3. **돌봄 무관 발화 처리** — Gemini 프롬프트에 규칙 추가: 잡담 등 무관한 말로 관찰
   사실을 지어내지 않고, 짧게 인정한 뒤 담당 어르신 이야기로 되돌아오는 질문을
   하도록 명시. (데모 규칙기반 엔진은 이 판단을 재현할 수 없어 적용하지 않음 —
   실 Gemini 경로에서만 검증 가능하고 이 환경에서는 실기 검증 못 함.)
4. **관리자 화면 우선순위 재배치** — "센터가 확인할 돌봄"(우선 확인 필요/미확인/오늘
   완료/오늘 참여) 요약을 최상단에 배치하고, 기존 연구용 KPI 그리드는 `<details>`로
   접어 필요할 때만 펼치게 했다. 보고 목록에 관찰·대응·센터 확인사항 요약과 AI
   처리상태 배지를 추가(빈 값은 "미기록"/"확인 필요"로 표시, "문제없음"으로 바꾸지
   않음).
5. **지표 정의 점검·수정**(`shared/statsCalc.ts`):
   - 구조화 완료율: 시작한 지 1시간 안쪽인 draft("진행 중")는 분모에서 빼고 방치된
     draft만 실패로 집계, 완료/방치/진행중 내역을 함께 노출.
   - 완료시간: "시작~제출 전체 경과시간"이라고 라벨을 정정(대기시간이 섞일 수 있음을
     명시)하고, 30분 초과 건은 제외 건수를 밝힌 채 별도 중앙값을 함께 표시(과거
     데이터로 실제 상호작용 시간 자체는 계산 불가 — 추정해서 채우지 않음).
   - 재사용률: 첫 제출일이 오늘인 참여자는 재사용 기회가 아직 없었을 수 있어 분모에서
     빼고 "관찰 중" 인원으로 별도 표시(0%로 뭉개지 않음).
   - AI 폴백 발생률 신설(위 2번).

### 검증
- `npx tsc -b`, `vite build`, `oxlint`, `vitest run`(83→86건, 신규 3건 포함) 통과.
- `playwright test e2e/care-flow.spec.ts`(mobile-390/360, 신규 회귀 테스트 포함) 통과 —
  회귀 테스트는 수정 전 코드로 되돌려 실패하는 것까지 직접 확인한 뒤 원복.
- 데모 모드 브라우저 실클릭으로 관리자 화면 신규 섹션(우선순위 요약, 접이식 KPI,
  목록 콘텐츠 요약, 폴백 배지) 렌더링 확인.
- 실제 Gemini/Supabase 운영 경로, 돌봄 무관 발화 응답, 실제 두 건(C07/C09)의 단계별
  로그 추적은 이 환경에 DB/로그 접근 권한이 없어 미검증 — 세션 최종 보고 참고.

### 배포
`db/schema.sql`에 `ai_fallback_used`/`ai_fallback_stage` 컬럼을 멱등 `alter table`로
추가했다 — 실제 Supabase에는 자동 적용되지 않으므로 배포 전/후 관리자가 SQL Editor에서
직접 실행해야 한다(기존 컬럼 추가와 동일한 절차, PILOT_LAUNCH_CHECKLIST.md 참고).

## 2026-09-07 — 요양보호사-수급자 배정 + 핵심 대화→기록→검토 루프 강화

### 배경
9/18 데모데이에서 보여줄 핵심 가치 하나에 집중: "요양보호사가 편하게 말하면 → AI가
필요한 것만 확인해 기록으로 정리하고 → 관리자가 검토해 활용한다." 이번 회차는 전체
재작성이 아니라, 기존에 잘 동작하던 파일럿 코드(요양보호사 화면, 관리자 화면,
`careReportAi.ts`, `noChangeEngine.ts`) 위에 이 핵심 루프를 끊는 부분만 보강했다.

### 만든 것
1. **요양보호사-수급자(C-A) 배정 관계** — `db/schema.sql`의 `caregiver_assignments`
   다대다 테이블, `demoAssignedRecipients()`. 요양보호사는 자신에게 배정된 수급자만
   보고 접근할 수 있다(다른 요양보호사 수급자로 폴백 없음). 배정 1명이면 자동 연결,
   2명 이상이면 "대상자 변경" 보조 UI만 제공. `CareApp.tsx` 홈 화면의 대형 드롭다운을
   제거하고 "AI와 대화 시작" 버튼을 메인 CTA로 배치.
2. **응급 즉시감지** — `shared/emergency.ts`(옛 `src/triage.ts`의 정규식 5종을
   그대로 포팅, 절 단위 부정문 오탐 완화). 최초 발화뿐 아니라 일반 흐름·특이사항없음
   흐름의 모든 추가답변에도 적용. 감지 시 119/센터 연락 화면(`emergency` screen)으로
   전환하되 자동 제출하지 않고, `emergency_flagged`를 draft 상태에서도 즉시 저장한다
   (전달 성공/검토완료 집계에는 포함하지 않음). 정규식 기반이라 완벽하지 않고
   "검증된 임상 기준"이 아님을 화면에도 명시.
3. **caregiverNote(요양보호사 상황·지원요청) 분리** — `StructuredReport`에 5번째
   필드 추가. `shared/caregiverNote.ts`가 어려움 표현 주변 절에서 3인칭 주어(어르신
   등)·존댓말 활용형(힘드셨/힘들어하셨 등)이 있으면 관찰 사실로, 아니면(1인칭이거나
   주어 생략) 요양보호사 본인 경험으로 분류한다. 특이사항없음 흐름도 무조건 빈
   문자열로 두지 않고 동일 기준으로 추출(`buildNoChangeReport`에 rawTexts 인자 추가).
   완벽한 자연어이해가 아닌 규칙 기반 휴리스틱이라는 한계는 유지.
4. **조기종료 존중** — "여기까지 말씀드릴게요" 버튼(질문 화면) + `shared/stopRequest.ts`
   (발화 안의 "그만할게요"류 표현 감지, 어르신 말 인용은 제외). 감지되면 현재
   답변(정보)은 먼저 반영한 뒤 남은 질문만 건너뛴다. `api/_lib/careReportAi.ts`·
   `api/care/ai-turn.ts`·데모 엔진에 `forceFinalize` 파라미터 추가.
5. **관리자 검토(승인/반려) 워크플로우** — 기존 1/2단계 연구용 평가와 별개.
   `reports`에 `admin_final_report`/`review_status`/`review_note`/`reviewed_at`/
   `review_history`/`last_review_request_id` 추가. 승인/반려 시 폼 내용을 항상
   `admin_final_report`에 스냅샷으로 저장(확정본 없음=미검토 라는 모호함 제거),
   화면에는 승인본/반려 시 검토본을 다른 스타일로 구분 표시. 조건부 갱신(CAS,
   `expectedUpdatedAt`)과 중복요청 무시(`requestId`)를 적용 — 실DB 경로는 UPDATE
   문 자체에 조건을 걸고, 데모(localStorage) 경로는 계약만 모의 구현(진짜 원자적
   CAS 아님, 자동테스트로 계약만 검증). 개별 검토자 신원은 기록하지 않는다(공유
   관리자 비밀번호뿐이라 확인 불가 — 처리시각/이전·이후 상태/반려사유/행위주체
   범위 문자열만 이력에 남김).
6. **관리자 기본 3수치** — 저장된 돌봄기록 수 / 전달 성공(제출) / 검토완료(승인+반려,
   제출된 것만). `shared/statsCalc.ts`의 연구용 KPI와는 분리, 새로 재정의하지 않음.
7. **하위호환** — `shared/careTypes.ts`의 `normalizeReportRecord()`가 저장소 경계에서
   새 필드 기본값을 채운다(옛 데모 데이터가 있어도 화면이 깨지지 않음).

### 검증
- `npm run build`, `npx vitest run`(기존 49개 + 신규 32개 = 81개) 통과.
- 브라우저(데모 모드, `?demo=1`, 같은 브라우저 두 탭)로 시나리오 A~H, D, J, K, L을
  실제 클릭으로 확인(정확한 결과는 세션 최종 보고 참고). 실제 Gemini API·실DB
  동시성은 이 환경에 키/연결이 없어 미검증 — 데모 규칙기반 엔진으로만 검증했다.
- 자동테스트 작성 중 실제 버그 발견·수정: `demoUpdateReport`의 CAS 토큰이
  `new Date().toISOString()`(밀리초 정밀도)이라 빠른 연속 갱신 시 값이 겹쳐 충돌
  감지가 무력화되는 경우가 재현됨 → 단조증가 타임스탬프 발급 함수로 교체
  (`demoStore.ts`의 `nextTimestamp()`).

### 남은 제한
- 실제 Supabase DB에는 스키마 변경분을 적용하지 않았다(연결된 실DB 없음) —
  `db/schema.sql` 파일만 갱신, 실제 배포 전 관리자가 SQL Editor에서 직접 실행 필요.
- caregiverNote/응급감지/종료의사 감지는 모두 규칙 기반 휴리스틱이며, 100% 정확한
  자연어이해가 아니다.
- 데모 모드는 같은 브라우저 내 탭 간(localStorage+BroadcastChannel)만 동기화된다 —
  기기 간 동기화는 지원하지 않으며, 이번 범위에서 새 백엔드도 만들지 않았다.

## 2026-09-04 (2차) — 데모 모드, 투자자 대시보드, "특이사항 없음" 대응 흐름

### 배경
Supabase·Gemini 실계정 없이도 사용자가 실제 브라우저에서 전체 흐름을
직접 시연·검증할 수 있어야 한다는 요구에 따라, 외부 서비스에 전혀
의존하지 않는 **데모 모드**를 추가했다. 동시에 관리자 화면을 투자자
피칭이 가능한 실증 대시보드로 확장하고, 실제 현장에서 가장 흔할 "평소와
비슷했어요"(특이사항 없음) 상황을 강제 유도 없이 구조화하는 흐름을 넣었다.

### 만든 것
- **데모 모드** (`?demo=1`) — `src/pilot/demo/*`. Supabase/Gemini 없이
  localStorage + BroadcastChannel만으로 `/care?demo=1`, `/admin?demo=1`,
  `/admin/presentation?demo=1`, `/care/scenario?demo=1` 전체 흐름이 그대로
  작동한다. 참여자 PIN은 전부 `1234`, 관리자 비밀번호는 `demo1234`(화면에
  안내 표시). AI 질문·구조화는 규칙 기반 엔진(`demoAiEngine.ts`)이 대신하며,
  모든 화면에 "DEMO DATA · 실제 실증 결과가 아닙니다" 배지를 고정 표시한다.
  "데모 초기화" 버튼으로 언제든 리셋 가능. 실제 API 코드(`api/*`, DB 스키마)는
  전혀 건드리지 않고 완전히 분리된 경로로 구현했다.
- **공용 로직 재배치** — `api/_lib/statsCalc.ts`, `csv.ts`를 저장소 루트의
  `shared/`로 옮겨 서버(api/, tsconfig.api.json)와 브라우저(src/,
  tsconfig.app.json) 양쪽에서 동일한 통계·CSV 로직을 그대로 재사용한다
  (데모 모드가 실제 서버와 동일한 계산식을 쓰도록 보장하기 위함).
- **관리자 실증 대시보드 확장** — `src/pilot/admin/AdminApp.tsx`,
  `charts.tsx`. 실증 헤더(기관명, 기간, LIVE 배지), 6개 핵심 KPI 카드(분자/
  분모 항상 표시), 순수 SVG 막대·선 그래프(전후 비교, 일자별 누적), 참여자별
  표+히트맵, 실시간 돌봄보고 피드(3초 폴링), 전후 비교 상세, 투자자 피칭
  화면(`/admin/presentation`, 민감정보 제외), 지표 정의 툴팁(ⓘ). 실제
  데이터가 없을 때는 카드에 `측정 전`/`0 / 9명`처럼 표시하고 그래프에만
  "제출되면 표시됩니다" 안내를 띄운다(사양서 14번 요구사항).
- **"특이사항 없음" 대응 흐름** — `shared/noChangeEngine.ts`(규칙 기반,
  Gemini 불필요), `src/pilot/care/CareApp.tsx`. 돌봄보고 시작 후 "평소와
  다른 점 있음 / 평소와 비슷함 / 확인 필요 / 직접 말하기" 3+1 선택지를
  먼저 제시하고, "특이사항 없음" 계열 발화는 자동 감지해 같은 흐름으로
  연결한다. 이 흐름은 고정 질문 최대 2개(유도질문 없음)로 9개 관찰영역
  (식사·수분/이동·낙상/배설/인지·의사소통/정서·행동/통증·호흡/수면/피부·
  위생/복약 관찰)을 `same_as_usual/changed/not_observed/uncertain` 중 하나로
  분류하고, 언급되지 않은 영역은 절대 "정상"으로 바꾸지 않는다.
- **표준상황 연습** (`/care/scenario`) — 고정된 가상 상황 2개, `report_source
  ='scenario'`로 실제 현장보고와 완전히 분리 저장·집계(`computeScenarioStats`).
  필수정보 확인률·사실 생성 여부는 시나리오별 정답 키워드로 자동 채점한다.
- **DB 스키마 확장** — `db/schema.sql`에 특이사항 없음 흐름 필드(13개) 추가,
  기존 테이블에도 안전하게 적용되도록 `alter table ... add column if not
  exists` 멱등 블록을 포함했다.
- **Playwright E2E** — `playwright.config.ts`, `e2e/*.spec.ts`. 데모 모드
  기준 골든 패스(요양보호사 제출→관리자 확인→평가→지표 갱신), 권한 격리,
  새로고침 후 데이터 유지, 특이사항 없음/표준상황 분리를 모바일 뷰포트
  (390×844, 360×800)에서 검증한다.

### 실제 브라우저 시연 중 발견하고 고친 버그 (중요)
"코드가 있다"와 "실제로 작동한다"는 다르다는 지적에 따라, 이번에는 로컬
브라우저에서 데모 모드 전체 흐름을 직접 클릭해 검증했고, 그 과정에서
실제로 3개의 버그를 발견해 고쳤다(자동 테스트만으로는 못 잡았을 것들):
1. 제출 직후 "센터에 보고되었습니다." 화면이 뜨자마자 홈으로 튕기던 버그
   (`loadHome`이 화면 상태까지 덮어씀) → `navigateToHome` 플래그로 분리.
2. "평소와 비슷했어요"를 눌러도 자유 입력 화면으로 새는 버그 → 바로 고정
   질문으로 연결하도록 수정.
3. 모든 분수 표시(`OO건 중 OO건, N%`)의 분자/분모 순서가 사양서 예시와
   반대였던 버그 → 표기 규칙을 통일해 수정.

### 왜 이렇게 했는가
- **데모 모드에 서버를 두지 않은 이유**: Vercel 서버리스 함수는 요청마다
  상태가 사라지므로, 외부 계정 없이 "즉시 시연 가능"하려면 순수 브라우저
  저장(localStorage)만으로 완결되는 구조가 유일하게 확실한 선택이었다.
  대신 같은 브라우저의 다른 탭끼리만 실시간 동기화되고, 시크릿창과는
  동기화되지 않는다는 한계를 화면·문서에 명시했다.
- **특이사항 없음 분류를 규칙 기반으로 만든 이유**: 질문 문구가 사양서에서
  이미 고정 문장으로 지정돼 있어 AI가 재량으로 바꿀 이유가 없고, 오히려
  규칙 기반으로 만들면 Gemini 키가 없어도(그리고 데모 모드에서도) 100%
  동일하게 동작해 이 흐름 전체가 외부 의존성 없이 신뢰 가능해진다.

## 2026-09-04 — 실증 파일럿 최초 구현

### 배경
기존 확장형 MVP 개발을 일단 중단하고, 단일 가설 검증을 위한 초경량 실증 MVP를
별도 브랜치에 새로 구현했다. 가설: "요양보호사가 방문 후 관찰한 내용을 말하면 AI가
부족한 정보를 되물어, 센터가 추가 질문 없이 바로 판단할 수 있는 구조화된 돌봄보고를
만들 수 있다."

### 만든 것
- **DB 스키마** ([db/schema.sql](db/schema.sql)) — Supabase(Postgres). `participants`,
  `recipients`, `reports`, `admin_audit_log` 4개 테이블. RLS 활성화, 접근은 서버의
  Service Role 키로만 허용(브라우저 직접 접근 불가).
- **서버 API** (`api/care/*`, `api/admin/*`, `api/_lib/*`) — Vercel Node 서버리스
  함수. 참여자 PIN 로그인/세션(JWT httpOnly 쿠키, 14일), 관리자 비밀번호
  로그인(bcrypt 해시 비교), 돌봄보고 작성·조회·제출, AI 질문/구조화 턴
  (`api/_lib/careReportAi.ts`, Gemini `gemini-3.6-flash`, JSON 스키마 강제, 최대
  3회 질문 서버 강제), 관리자 2단계 블라인드 평가, 통계 집계
  (`api/_lib/statsCalc.ts`), CSV 내보내기(요약/전체), 참여자 PIN 초기화, 보고
  소프트 삭제.
- **/care 화면** ([src/pilot/care/CareApp.tsx](src/pilot/care/CareApp.tsx)) — 참여자
  코드+PIN 로그인 → 홈(오늘 상태, 수급자 선택, 기본/추가 보고 버튼, 최근 목록) →
  음성/텍스트 입력(둘 다 항상 노출) → AI 추가 질문(최대 3회, 한 번에 하나) →
  최종 보고문 확인·수정 → 제출 → "센터에 보고되었습니다." 화면. 기기 내
  localStorage + 서버 draft 이중 저장으로 새로고침/네트워크 오류 시 복구.
- **/admin 화면** ([src/pilot/admin/AdminApp.tsx](src/pilot/admin/AdminApp.tsx)) —
  비밀번호 로그인 → 대시보드(핵심지표, 목표 진행률, 전후 비교, 참여현황표, 8초
  폴링 갱신) / 보고 목록·상세(1단계 원문 블라인드 평가 → 저장 후 2단계 AI 보고
  평가 공개, 재평가 시 이력 보존) / 참여자 관리(PIN 초기화) / CSV 다운로드.
- **라우팅**: `src/main.tsx`에 `/care`, `/admin` 두 경로를 추가하고 이 브랜치에서만
  `/care`를 파일럿 화면에 연결했다. 기존 `src/care/CareWorkspace.tsx` 파일은 삭제·
  수정하지 않고 그대로 두었다(더 이상 어떤 경로에서도 불러오지 않을 뿐). 다른
  브랜치/배포본은 영향을 받지 않는다.
- **테스트**: `vitest` 도입, 순수 집계 로직(`statsCalc.ts`, `csv.ts`) 단위테스트
  15개 작성 및 통과. `npm run build`(tsc + vite), `npm run lint`(oxlint) 통과.

### 왜 이렇게 했는가 (주요 설계 판단)
- **실시간 갱신 = 폴링(8초)**: Supabase Realtime/커스텀 JWT 기반 RLS 대신, 참여자
  9명·평가 90여 건 규모의 단기 실증에서는 8초 폴링으로도 "새로고침 없이 반영"
  요구를 충분히 만족하면서 구현 복잡도와 실패 지점을 크게 줄일 수 있다고 판단했다.
- **DB 접근은 항상 서버 경유**: Supabase anon 키를 브라우저에 두지 않고, 모든
  읽기/쓰기를 Vercel 서버리스 함수(Service Role 키)로만 수행한다. RLS는 활성화만
  해두고 정책은 추가하지 않아 기본적으로 모든 직접 접근을 차단한다.
- **"수정 없이 사용된 보고 비율"은 자동 비교로 계산**: 관리자가 별도로 "수정 필요
  정도"를 입력하지 않고, `ai_generated_report`와 `caregiver_final_report`를 서버가
  직접 비교해 집계한다. 관리자 평가 항목 수를 늘리지 않기 위한 선택.
- **AI 질문 3회 제한은 서버가 이중으로 강제**: 프롬프트에 규칙을 명시하는 것과
  별개로, 서버가 4번째 턴부터는 무조건 `needFollowup=false`로 마무리하고, AI가
  보고문 생성에 실패해도 원문 그대로를 담은 대체 보고문(fallback)을 만들어 흐름이
  끊기지 않게 했다.
