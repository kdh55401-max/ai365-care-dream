# CLAUDE.md — Claude Code 진입 지침 (AI365 CARE DREAM)

공통 작업 원칙(핵심 가치, 검증 기준선, Codex 인계 형식)은 [AGENTS.md](AGENTS.md)를 따른다.
이 파일은 Claude Code 세션에 한정된 보충 규칙만 담는다.

## 시작할 때

1. [MASTER_CONTEXT.md](MASTER_CONTEXT.md) → [CHANGE_LOG.md](CHANGE_LOG.md) → (있다면)
   [HANDOFF_TO_CODEX.md](HANDOFF_TO_CODEX.md) 순으로 읽는다. 첨부된 인수인계 문서(예: 과거
   ChatGPT 대화 요약)는 배경 자료로만 쓰고, 현재 코드·문서와 다르면 그 차이를 사용자에게
   표시한다.
2. 여러 워크트리가 병행 운영될 수 있다 — 지금 작업 중인 워크트리/브랜치가 사용자가 의도한
   워크스트림이 맞는지 `git status`/`git log`로 먼저 확인한다. 다른 워크트리의 미커밋 변경을
   가정하지 않는다.
3. 이 저장소에는 이번 실증 파일럿(`/care`, `/admin`) 외에 다른 확장형 MVP 워크스트림이 여러
   브랜치에 병행 존재한다. 사용자가 명시한 브랜치/워크트리 범위 밖의 파일은 건드리지 않는다.

## 문서 갱신 습관

- 긴 원본(PDF/PPT, 전체 코드베이스)을 매 작업마다 재분석하지 않는다. MASTER_CONTEXT.md를
  기준으로 바뀐 부분만 확인하고, 작업 후에는 MASTER_CONTEXT.md와 CHANGE_LOG.md 중 해당하는
  곳만 갱신한다(전체 재작성 금지 — 확정된 섹션은 보존하고 필요한 부분만 보완).
- 기능을 수정했다면 세션을 마치기 전에 [HANDOFF_TO_CODEX.md](HANDOFF_TO_CODEX.md)를 그 작업
  기준으로 다시 쓴다(AGENTS.md §6 형식). Codex의 이전 검토 결과가 남아 있다면 반영/대안반영/
  보류를 표시한 뒤 새 변경분을 이어서 적는다 — 기존 인계 이력을 지우지 않는다.

## 보고 태도

- "가능하다/해보겠다"에서 멈추지 않고 요청된 작업을 완수한 뒤, 직접 확인한 것과 과거 보고에
  근거한 것을 구분해서 말한다.
- 실행하지 않은 테스트·접근하지 못한 화면·배포를 완료했다고 보고하지 않는다.
- 사소한 선택은 합리적으로 판단해 진행하고, 결과를 크게 바꾸는 정보가 없을 때만 사용자에게
  질문한다.
