import App from '../App'

/**
 * 생활지원사(COMMUNITY) 업무공간. 기존 '현장 대응 도우미' 화면과
 * 생활안전스캐너 진입 카드를 그대로 보존한 App을 그대로 사용한다.
 * (App.tsx 헤더에 '역할 전환' 링크만 추가했다.)
 */
function CommunityWorkspace() {
  return <App />
}

export default CommunityWorkspace
