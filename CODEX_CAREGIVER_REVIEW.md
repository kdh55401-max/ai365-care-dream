# AI365 CARE DREAM — 아바타 대화 화면 검증

## 2026-09-11 구현

기준: 원격 master `703d502`. 시작 시 작업 트리 깨끗함. 사용자의 첨부 아바타를 실제 서비스 자산으로 사용하라는 이번 지시가 과거의 '얼굴 없는 추상 아바타' 원칙을 대체한다.

- 제공된 `ai365 초안.png`를 `public/assets/ai365-companion.png`로 그대로 복사했다. SHA-256: `C6BCCFB2ED3FF29F71ED6442188FCA365F20B5459EA52EAC41171C6E35098776`. 워터마크를 포함한 원본을 유지했고 새 이미지/영상 생성은 하지 않았다.
- 큰 아바타·실제 배정 대상·짧은 인사·이야기 시작을 홈의 중심으로 재구성했다. 기록/계정/연습은 보조 영역이다. 배정 선택은 기존 세션과 대상자별 제출 판정을 그대로 사용한다.
- 홈과 대화 사이에 같은 이미지 DOM을 유지한다. 620ms 위치/크기 전환 후 대화 영역을 표시한다. 390px 캡처에서는 약 250px→80px, y 약 396→126으로 이동하고 `sameNode: true`다. 움직임 줄이기에서는 전환을 생략한다.
- 실제 입력과 응답 이력, 현재 AI 질문을 구분한다. 무변화 문답/실제 질문 횟수는 로컬 초안에 함께 저장해 새로고침 후 복원한다. DB 구조나 information_added_count 정의를 변경하지 않았다.
- TTS 재생은 start/end/error에 연동하며 취소된 이전 발화의 늦은 이벤트를 무시한다. 마이크 시작 전에 재생을 취소한다. 정지 이미지이며 립싱크는 구현하지 않았다.
- 후속 질문 실패 시 실제 대화 이력·오류·재시도를 함께 보여준다. 최종확인은 원문을 보존해 보여주고 보고문을 수정한 후 '센터에 보고하기'로 제출한다. 저장 성공 뒤에만 완료 화면으로 이동한다.
- 입력 영역은 18px 글자, 44~56px 이상 주요 터치 영역, 하단 보고 버튼, visualViewport 크기 변경 시 입력창 스크롤을 적용했다. 실기기 키보드 확인과 브라우저의 작은 viewport 검증은 구분한다.

## 검증 증거

- `src/pilot/care/speechOutput.test.ts`: 실제 TTS 이벤트/취소/지연 이벤트/실패 처리 회귀.
- `e2e/companion-redesign.spec.ts`: 같은 아바타 이동, 배정 대상, 대화와 보고 수정, 관리자 연결, 작은 viewport, 후속 AI 실패/재시도, 마이크와 TTS 상태, 권한 거부, reduced motion, 무변화 문답 복원, 보고 저장 실패/중복 방지.
- 기존 E2E는 바뀐 버튼·안내 문구와 접힌 계정 메뉴 접근만 갱신했다. 대상자별 완료, 관리자 평가, 역할 경로, 데모 분리 검사를 유지한다.
- 화면 캡처: [홈](docs/caregiver-redesign/home.png), [대화](docs/caregiver-redesign/conversation.png), [보고 확인](docs/caregiver-redesign/review.png).
- [화면 전환 녹화](docs/caregiver-redesign/transition.webm). 화면 조작 녹화이며 생성 영상이나 립싱크 영상이 아니다.

## 보존 범위 및 미검증

`api/`, `shared/`, `db/`, 관리자 화면과 서버 역할 검증은 이번 구현에서 변경하지 않는다. 기존 질문 제한(일반 최대 3회/무변화 최대 2회), 도메인 기반 지표, 관리자 수정·승인·반려 이력, 실제/데모 분리를 유지한다.

검증은 데모 계정과 테스트 브라우저 저장소를 사용하며 운영 돌봄 기록에 시험 데이터를 쓰지 않는다. 실제 인증 계정으로 Gemini/Supabase 통합, 한국어 음성 실제 청취·인식 정확도, iOS/Android 실기기 키보드/스피커 재인식은 미검증이다. 마이크/TTS 이벤트 검사는 모의 API 기반이며 실기기 음질 검증을 의미하지 않는다.

## 최종 로컬 검증 결과

- `npm run build`: TypeScript/Vite 성공. `npm test`: **117/117 통과** (10개 파일). lint 오류 0, 기존 경고 3개 유지.
- **E2E 총 60개 케이스의 통과를 확인했다.** 전체 실행은 57 통과/3 실패였다. 그중 2건은 관리자 원문과 편집칸에 같은 문장이 있어 생긴 테스트 선택자 중복으로, 원문 영역을 특정한 뒤 2/2 재실행 통과. 나머지 1건은 실행 중 실제 마이크 시작 이벤트 분리를 반영한 뒤 옛 모의 인식기가 start 이벤트를 보내지 않은 경우로, 갱신한 이벤트 테스트가 두 화면 크기에서 통과했다. 최종 동결 소스에서 새 기능 12개를 재실행해 10개 통과, 위 선택자 보정 2개도 재실행 통과했다. 단일 실행 60/60으로 기록하지 않는다.
- 모바일 Chromium 390×844/360×800, 작은 viewport 450px, 실제 이미지 로딩, 같은 DOM/좌표/크기 변화, reduced motion, 인식 권한 거부와 텍스트 폴백, TTS start/end/error, 요청·저장 실패, 새로고침 복원을 확인했다. 실제 모바일 키보드를 띄운 검증은 아니다.
- 데모 작성자 C01/대상 A01/입력 원문/사용자가 수정한 `현재 상태`가 저장된 보고에서 일치하고 관리자 상세에서 동일 작성자·대상자·원문이 조회되는 것을 확인했다.
- 배포 전 운영 주소의 읽기 전용 점검: `/care?demo=1` HTTP 200, 비로그인 `/api/care/session`은 `authenticated:false`, `/api/admin/reports`는 HTTP 401. 실제 로그인 후 API/DB 통합 성공을 의미하지 않는다.
- 390px 화면 녹화에서 같은 이미지 요소가 x≈70/y≈396/폭≈250에서 x=18/y=126/폭=80으로 이동했다. 캡처 중 페이지 오류 0, 실제 `/api/` 호출 0.

작업 브랜치: `codex/avatar-conversation-20260911`. 배포 주소: https://ai365-care-dream.vercel.app/care. 구현 커밋은 이 문서와 함께 Git 이력에 보존한다. 실제 Production 배포 SHA는 Vercel/GitHub deployment 상태와 대조해야 하며 빌드 성공만으로 배포 성공을 추정하지 않는다.
